# 04_部署指南

**文档版本**: v9.0
**最后更新**: 2026-08-15

---

## 一、环境准备

### 1.1 必需软件

| 软件 | 版本要求 | 说明 |
|------|----------|------|
| JDK | 21+ | Java 运行环境 |
| Node.js | 18+ | 前端构建工具 |
| MySQL | 8.0+ | 关系型数据库 |
| Redis | 6.0+ | **必需组件** — 登录态、验证码、限流、字典缓存 |
| Maven | 3.8+ | Java 项目构建 |
| Git | 最新版 | 版本控制 |

### 1.2 可选软件

| 软件 | 版本 | 说明 |
|------|------|------|
| MinIO | 最新版 | 对象存储（不装则降级为本地文件存储）|
| MongoDB | 6.0+ | 辅助中间件（可选，不影响核心功能）|
| Docker | 20+ | OJ 判题沙箱（不装则判题功能不可用，其他功能正常）|

### 1.3 Redis 必要性

> Redis 是**核心依赖**，不是可选组件。以下功能依赖 Redis：
> - 用户登录态（`login_tokens:{uuid}` / `portal_login_tokens:{uuid}`）
> - 图形验证码（`captcha_codes:{uuid}`，2 分钟过期）
> - 接口限流（`@RateLimiter`）
> - 防重复提交（`@RepeatSubmit`）
> - 密码错误锁定计数
> - 字典缓存

**没有 Redis → 系统完全不可用。**

### 1.4 环境验证

```bash
java -version        # JDK 21+
node -v              # Node.js 18+
mvn -version         # Maven 3.8+
mysql --version      # MySQL 8.0+
redis-cli ping       # 应返回 PONG
```

### 1.5 JDK 21 环境排查（v10.0 P0-4 新增）

> **背景**：Spring Boot 3.3.2 强制要求 Java 21+，但开发机/服务器上常见 PATH 优先级问题导致 `java -version` 实际指向 Java 8，编译时报 "class file version 65.0" 等错误。本节给出排查方法。

**排查步骤**（PowerShell）：

```powershell
# 1. 检查当前 java 版本（必须显示 21+，不是 1.8）
java -version

# 2. 检查 JAVA_HOME 环境变量
echo $env:JAVA_HOME   # 应指向 JDK21 安装目录，如 D:\dev_en\JAVA21

# 3. 检查 PATH 中 java 的实际来源
where.exe java        # 第一个匹配项必须是 JDK21\bin\java.exe
where.exe mvn         # Maven 必须指向真实 Maven 安装目录，而非 C:\Windows\System32\mvn

# 4. 如果 mvn 指向 System32（损坏的 shim），用全路径执行
& "D:\dev_en\apache-maven-3.9.14\bin\mvn.cmd" -version
```

**修复方法**（Windows 开发机）：

```powershell
# 临时（仅当前会话生效）
$env:JAVA_HOME = "D:\dev_en\JAVA21"
$env:PATH = "$env:JAVA_HOME\bin;$env:PATH"

# 永久（写入用户环境变量，需重启 PowerShell）
[Environment]::SetEnvironmentVariable("JAVA_HOME", "D:\dev_en\JAVA21", "User")
[Environment]::SetEnvironmentVariable("PATH", "$env:JAVA_HOME\bin;" + [Environment]::GetEnvironmentVariable("PATH", "User"), "User")
```

**编译验证命令**（使用全路径 mvn + 临时 JAVA_HOME，已验证通过 1190 源文件 BUILD SUCCESS）：

```powershell
$env:JAVA_HOME = "D:\dev_en\JAVA21"
$env:PATH = "$env:JAVA_HOME\bin;$env:PATH"
cd d:\zyg_new_work\moyun-project-document\moyun-server
& "D:\dev_en\apache-maven-3.9.14\bin\mvn.cmd" clean compile -DskipTests
```

**生产环境部署注意**：
- 必须在生产服务器上安装 JDK 21（不是 JRE 21）
- `JAVA_HOME` 必须在系统级环境变量中配置（非用户级），确保 systemd/service 启动的 Java 进程也能读到
- Docker 部署使用 `eclipse-temurin:21-jdk-alpine` 或 `amazoncorretto:21-alpine` 基础镜像
- K8s 部署在 Pod spec 中显式设置 `JAVA_HOME=/usr/lib/jvm/java-21-amazon-corretto`

---

## 二、数据库初始化

### 2.1 创建数据库

```sql
CREATE DATABASE moyun-db DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
```

### 2.2 执行初始化脚本

**脚本目录**：`moyun-server/src/main/resources/sql/`

**按编号顺序执行**（编号有跳号属正常，按现有编号顺序执行即可）：

```bash
cd moyun-server/src/main/resources/sql

# 第一阶段：建表（DDL，仅空库执行，已有数据的环境跳过此阶段）
mysql -u root -p moyun-db < 00_开头设置.sql
mysql -u root -p moyun-db < 10_基础系统表.sql
mysql -u root -p moyun-db < 20_工作流表_act.sql
mysql -u root -p moyun-db < 30_定时任务表_qrtz.sql
mysql -u root -p moyun-db < 40_代码生成表_gen.sql
mysql -u root -p moyun-db < 50_门户核心表.sql
mysql -u root -p moyun-db < 51_门户扩展表.sql
mysql -u root -p moyun-db < 52_门户面试学习表.sql
mysql -u root -p moyun-db < 53_门户互动社交表.sql

# 第二阶段：种子数据
mysql -u root -p moyun-db < 80_系统种子数据.sql
mysql -u root -p moyun-db < 81_门户种子数据.sql
mysql -u root -p moyun-db < 90_菜单权限_RuoYi.sql
mysql -u root -p moyun-db < 91_菜单权限_CMS.sql
mysql -u root -p moyun-db < 92_角色菜单关联.sql
mysql -u root -p moyun-db < 93_菜单权限_消息中心.sql

# 第三阶段：升级脚本（已有数据库的环境也要执行，幂等可重复执行）
mysql -u root -p moyun-db < 95_升级脚本_v6.4.sql
mysql -u root -p moyun-db < 96_升级脚本_v6.7.sql

# 第三阶段：v8.1 审核模块统一整合（sys_audit_task + 任务管理菜单，幂等可重复执行）
mysql -u root -p moyun-db < upgrade_v8.1_audit_unified.sql

# 第三阶段：v8.2 通用导入模板 + 题库导入导出权限（幂等可重复执行）
mysql -u root -p moyun-db < upgrade_v8.2_import_template.sql

# 第三阶段：v9.0 升级脚本（幂等可重复执行）
mysql -u root -p moyun-db < upgrade_v9.0_admin_refactor.sql

# 第三阶段：v9.5/v9.6 升级脚本（v9.5 恢复打赏流水Tab权限；v9.6 菜单收敛+27类业务字典，幂等可重复执行）
mysql -u root -p moyun-db < upgrade_v9.5_merge.sql
mysql -u root -p moyun-db < upgrade_v9.6_admin_optimize.sql

# 第四阶段：校验与收尾
mysql -u root -p moyun-db < 98_校验查询.sql
mysql -u root -p moyun-db < 99_结尾设置.sql
```

> ⚠️ **注意**：
> - DDL 脚本（10~53）使用 `DROP TABLE IF EXISTS`，**仅适合空库初始化**，已有数据的环境**切勿重跑**。
> - 升级脚本（95/96）使用 `information_schema` 判断，**幂等可重复执行**。
> - 96 脚本包含审核字段、敏感词表、菜单权限、索引、定时任务等 v6.7 全部改动。

### 2.3 验证数据库

```sql
USE moyun-db;
SHOW TABLES;  -- 应看到 sys_*/qrtz_*/act_*/portal_* 系列表

-- 验证 v6.7 审核字段
DESC portal_article;   -- 应含 auditor_id / audit_remark / audit_time
DESC portal_topic;      -- status 默认值应为 pending
DESC portal_column;     -- 应含 auditor_id / audit_remark / audit_time

-- 验证敏感词表
SELECT COUNT(*) FROM sys_sensitive_word;  -- 应有 7 条示例词
```

---

## 三、后端服务部署

### 3.1 配置文件说明

配置文件位于 `moyun-server/src/main/resources/`：

| 文件 | 用途 | 启动参数 |
|------|------|----------|
| `application.yaml` | 基础配置（所有环境共用） | 默认 |
| `application-dev.yaml` | 开发环境（环境变量回退） | `--spring.profiles.active=dev` |
| `application-local.yaml` | 本地开发（明文凭据，仅本机） | `--spring.profiles.active=local` |
| `application-prod.yaml.example` | 生产环境模板 | 复制为 `application-prod.yaml` 后使用 |

### 3.2 开发环境启动

```bash
cd moyun-server

# 方式一：本地 profile（零配置，明文凭据仅本机用）
mvn spring-boot:run -Dspring-boot.run.profiles=local

# 方式二：dev profile（通过环境变量注入凭据）
export MYSQL_PASSWORD=your_password
export TOKEN_SECRET=your-secret-key-at-least-64-chars-long-hs512
mvn spring-boot:run -Dspring-boot.run.profiles=dev
```

### 3.3 生产环境部署

```bash
# 1. 复制生产配置模板
cp application-prod.yaml.example application-prod.yaml

# 2. 通过环境变量注入凭据（切勿在配置文件中写明文）
export MYSQL_URL="jdbc:mysql://db-host:3306/moyun-db?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=Asia/Shanghai"
export MYSQL_USERNAME="moyun"
export MYSQL_PASSWORD="your_strong_password"
export REDIS_HOST="redis-host"
export REDIS_PASSWORD="your_redis_password"
export TOKEN_SECRET="openssl rand -base64 64 生成的密钥"
export DRUID_USERNAME="druid_admin"
export DRUID_PASSWORD="druid_strong_password"
export MINIO_ENDPOINT="http://minio-host:9000"
export MINIO_ACCESS_KEY="your_access_key"
export MINIO_SECRET_KEY="your_secret_key"
export MINIO_ACCESS_URL="http://minio-host:9000"

# 3. 打包并启动
mvn clean package -DskipTests
java -jar target/moyun-server-*.jar --spring.profiles.active=prod

# 4. 后台运行
nohup java -jar target/moyun-server-*.jar --spring.profiles.active=prod > logs/moyun-server.log 2>&1 &
```

### 3.4 验证后端

- 服务地址：http://localhost:8080
- Swagger 文档：http://localhost:8080/doc.html（生产环境通过 `KNIFE4J_PRODUCTION=true` 关闭）
- 健康检查：http://localhost:8080/actuator/health
- 默认账号：`admin` / `admin123`

---

## 四、门户前端部署（moyun-portal）

### 4.1 开发环境

```bash
cd moyun-portal
npm install        # 或 pnpm install
npm run dev        # 启动开发服务器，默认 3000 端口
```

环境变量（`.env.development`）：
```
VITE_API_BASE_URL=http://localhost:8080
```

### 4.2 生产环境构建

```bash
cd moyun-portal
npm install
npm run build      # 产物输出到 dist/
```

环境变量（`.env.production`）：
```
# 生产环境通过 Nginx 反向代理 /api → 后端 8080
VITE_API_BASE_URL=/api
```

### 4.3 Nginx 配置示例

```nginx
server {
    listen 80;
    server_name your-domain.com;

    # 门户前端静态资源
    location / {
        root /path/to/moyun-portal/dist;
        try_files $uri $uri/ /index.html;
    }

    # API 反向代理
    location /api/ {
        proxy_pass http://127.0.0.1:8080/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

---

## 五、管理后台部署（moyun-admin-vue）

### 5.1 开发环境

```bash
cd moyun-admin-vue
npm install        # 或 pnpm install
npm run dev        # 默认 80 端口
```

环境变量（`.env.development`）：
```
VITE_APP_BASE_API = '/dev-api'
```
开发代理配置在 `vite.config.js` 中，`/dev-api` → `http://localhost:8080`。

### 5.2 生产环境构建

```bash
cd moyun-admin-vue
npm install
npm run build:prod   # 产物输出到 dist/
```

### 5.3 Nginx 配置示例

```nginx
server {
    listen 80;
    server_name admin.your-domain.com;

    location / {
        root /path/to/moyun-admin-vue/dist;
        try_files $uri $uri/ /index.html;
    }

    location /prod-api/ {
        proxy_pass http://127.0.0.1:8080/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

---

## 六、功能验证

### 6.1 基础验证

| 测试项 | 操作 | 预期结果 |
|--------|------|----------|
| 后端启动 | 访问 http://localhost:8080/actuator/health | 返回 `{"status":"UP"}` |
| 后台登录 | admin / admin123 | 登录成功，进入后台首页 |
| 门户首页 | 访问 http://localhost:3000 | 显示首页内容 |
| 门户登录 | 注册账号后登录 | 登录成功 |

### 6.2 v6.7 新功能验证

| 测试项 | 操作 | 预期结果 |
|--------|------|----------|
| 话题审核 | CMS 后台 → 话题审核 | 待审核列表正常，通过/驳回生效 |
| 专栏审核 | CMS 后台 → 专栏审核 | 审核通过后专栏公开可见 |
| 敏感词管理 | CMS 后台 → 系统管理 → 敏感词管理 | CRUD + 刷新词树正常 |
| 敏感词拦截 | 文章评论包含敏感词 | 提示"评论内容包含违规信息" |
| 举报下架 | CMS 处理举报为"成立" | 对应内容自动下架 |

### 6.3 v9.0 重构验证

| 测试项 | 操作 | 预期结果 |
|--------|------|----------|
| PK表已删除 | `SHOW TABLES LIKE 'portal_pk%'` | 0 条记录 |
| 圈子表已删除 | `SHOW TABLES LIKE 'portal_circle%'` | 0 条记录 |
| Tip独立菜单未恢复 | `SELECT * FROM sys_menu WHERE perms LIKE 'portal:tip%' AND menu_type='C'` | 0 条记录（v9.5 仅恢复 F 类型按钮权限） |

### 6.4 v9.5 合并版验证

| 测试项 | 操作 | 预期结果 |
|--------|------|----------|
| Tip按钮权限已恢复 | `SELECT * FROM sys_menu WHERE perms LIKE 'portal:tip%'` | 2 条记录（portal:tip:list / portal:tip:query，menu_type='F'） |
| 交易管理双Tab | CMS 后台 → 商业化 → 交易管理 | "付费订单"+"打赏流水"两个Tab，打赏流水列表可查询 |
| 内容型首页（游客） | 门户首页未登录访问 | 文章轮播/精选/热门/分类/作者榜/友链正常展示 |
| 首页导流入口 | 首页点击读书/面试导流卡片 | 正确跳转读书空间/面试指南 |
| Monaco编辑器 | 门户发布页代码块编辑 | 编辑器正常加载，控制台无 worker 报错 |
| VIP管理 | CMS 后台 → 商业化 → VIP管理 | VIP套餐列表正常 |
| 钱包管理 | CMS 后台 → 商业化 → 钱包管理 | 钱包列表 + 交易流水双Tab正常 |
| 业务仪表板 | CMS 后台 → 业务看板 | 核心指标/趋势图/排行正常展示 |
| growth-config 5Tab | CMS 后台 → 成长 → 成长配置 | 5个Tab（规则/成就/徽章/日志/用户）正常 |
| 学习辅助菜单 | `SELECT COUNT(*) FROM sys_menu WHERE path='learn-aux' AND menu_type='C'` | 重新执行 v9.5 脚本后为 1（仅面试指南下），后台从"面试指南→学习辅助"进入（/interview/learn-aux），"内容管理/读书空间"下无此项 |
| 交易入口唯一 | `SELECT COUNT(*) FROM sys_menu WHERE menu_name='财务' AND parent_id=0` | 重新执行 v9.5 脚本后为 0（财务目录已删），交易查询仅"商业化→交易管理"一个入口，付费订单/打赏流水双Tab均正常 |
| 交易管理权限链 | `SELECT perms FROM sys_menu WHERE parent_id=(SELECT menu_id FROM sys_menu WHERE perms='cms:transaction:list') AND menu_type='F'` | 4 条 F 按钮：portal:order:list/query + portal:tip:list/query（Tab 内两组件接口权限载体） |
| v9.6 菜单收敛 | 智能AI→知识中心（Tab容器正常渲染）；任务管理（仅定时任务+扫描结果可见）；内容管理（新增征文活动/写作提示词/认证审核直挂） | 知识中心点击不空白；"我的待办/我的已办"不在侧边栏；征文活动菜单可进 |
| v9.6 业务字典 | `SELECT COUNT(*) FROM sys_dict_type WHERE dict_type LIKE 'portal\_%' OR dict_type LIKE 'cms\_%' OR dict_type='sys_login_type'` | 27 类/104 条；后台订单页状态下拉 5 态（含已关闭/支付失败）；前台题库难度/题型、简历分类、举报反馈类型、AI场景由字典驱动（改字典前后台同步生效） |
| 话题发布 | 登录后直接发话题 | 无需创作者认证即可发起 |
| MobileTabBar | 移动端底部导航 | "面试"标签替代"读书"标签 |

---

## 七、常见问题

### 7.1 数据库连接失败

- 确认 MySQL 已启动
- 检查 `application-*.yaml` 中 `spring.datasource.url/username/password`
- 确认数据库 `moyun-db` 已创建

### 7.2 Redis 连接失败

- 确认 Redis 已启动：`redis-cli ping` 应返回 `PONG`
- 检查 Redis 密码配置

### 7.3 Token 相关报错

- 开发环境：`TOKEN_SECRET` 未设置会回退到 dev 默认密钥
- 生产环境：`TOKEN_SECRET` 未设置或长度不足 64 字符会 **fail-fast 拒绝启动**
- 生成密钥：`openssl rand -base64 64`

### 7.4 菜单不显示 / /getRouters 报错

- 确认 96 脚本已执行（菜单数据在 sys_menu 表）
- 确认 `sys_role_menu` 角色关联已建立
- admin 用户走 isAdmin 旁路，能看到所有菜单

### 7.5 逻辑删除失效

- 确认 `application-*.yaml` 中 `mybatis-plus.global-config.db-config.logic-delete-field` 为 `delFlag`
- 确认 `logic-delete-value` 为 `"2"`，`logic-not-delete-value` 为 `"0"`
- BaseEntity 的 `@TableLogic` 注解会覆盖全局配置，但全局配置需保持一致

### 7.6 Druid 监控页无法访问

- 地址：http://localhost:8080/druid/
- 凭据通过 `DRUID_USERNAME` / `DRUID_PASSWORD` 环境变量配置
- 生产环境 IP 白名单通过 `DRUID_ALLOW_IP` 配置（默认 127.0.0.1）

---

## 八、升级指南

### 从旧版本升级

已有数据库的环境，只需执行升级脚本（幂等可重复执行）：

```bash
mysql -u root -p moyun-db < 95_升级脚本_v6.4.sql
mysql -u root -p moyun-db < 96_升级脚本_v6.7.sql
```

96 脚本包含：
- 审核字段（auditor_id / audit_remark / audit_time）
- 话题状态默认值改为 pending
- 敏感词表（sys_sensitive_word / sys_sensitive_word_log）
- 菜单权限（话题审核 / 专栏审核 / 敏感词管理 / 话题管理）
- 定时任务（敏感词扫描）
- 索引补充
- ryTask 测试任务永久禁用
- 敏感词示例词

---

**文档维护**: 墨韵开发团队
