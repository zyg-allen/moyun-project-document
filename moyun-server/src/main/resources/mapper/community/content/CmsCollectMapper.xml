<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.moyun.community.content.mapper.CmsCollectMapper">

    <resultMap type="com.moyun.community.content.entity.CmsCollect" id="CmsCollectResult">
        <id property="id" column="id" />
        <result property="userId" column="user_id" />
        <result property="articleId" column="article_id" />
        <result property="status" column="status" />
        <result property="createTime" column="create_time" />
        <result property="createBy" column="create_by" />
        <result property="updateTime" column="update_time" />
        <result property="updateBy" column="update_by" />
    </resultMap>

    <select id="selectCollectListByUser" resultMap="CmsCollectResult">
        select id, user_id, article_id, status, create_time, create_by, update_time, update_by
        from cms_collect
        where user_id = #{userId}
        order by create_time desc
    </select>

    <select id="selectByUserAndArticle" resultMap="CmsCollectResult">
        select id, user_id, article_id, status, create_time, create_by, update_time, update_by
        from cms_collect
        where user_id = #{userId} and article_id = #{articleId}
        limit 1
    </select>

    <update id="updateStatus">
        update cms_collect
        set status = #{status}, update_time = sysdate()
        where id = #{id}
    </update>

</mapper>