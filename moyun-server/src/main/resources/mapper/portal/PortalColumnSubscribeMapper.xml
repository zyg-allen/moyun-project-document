<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.moyun.portal.mapper.PortalColumnSubscribeMapper">

    <select id="selectByColumnAndUser" resultType="com.moyun.portal.domain.entity.PortalColumnSubscribe">
        SELECT * FROM portal_column_subscribe WHERE column_id = #{columnId} AND user_id = #{userId}
    </select>

    <select id="countByColumn" resultType="int">
        SELECT COUNT(*) FROM portal_column_subscribe WHERE column_id = #{columnId}
    </select>

</mapper>
