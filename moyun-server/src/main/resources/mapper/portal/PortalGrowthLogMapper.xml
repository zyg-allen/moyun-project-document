<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.moyun.portal.mapper.PortalGrowthLogMapper">

    <select id="countTodayAction" resultType="int">
        SELECT COUNT(*) FROM portal_growth_log WHERE user_id = #{userId} AND module = #{module} AND action = #{action} AND DATE(create_time) = #{today}
    </select>

    <select id="countAction" resultType="int">
        SELECT COUNT(*) FROM portal_growth_log WHERE user_id = #{userId} AND action = #{action}
    </select>

    <select id="selectRecentLogs" resultType="com.moyun.portal.domain.entity.PortalGrowthLog">
        SELECT * FROM portal_growth_log WHERE user_id = #{userId} ORDER BY create_time DESC LIMIT #{limit}
    </select>

</mapper>
