<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.moyun.portal.mapper.PortalInterviewCommentMapper">

    <resultMap type="com.moyun.portal.domain.entity.PortalInterviewComment" id="PortalInterviewCommentResult">
        <id     property="id"             column="id"             />
        <result property="experienceId"   column="experience_id"  />
        <result property="userId"         column="user_id"        />
        <result property="parentId"       column="parent_id"      />
        <result property="replyToUserId"  column="reply_to_user_id" />
        <result property="content"        column="content"        />
        <result property="likeCount"      column="like_count"     />
        <result property="sort"           column="sort"           />
        <result property="status"         column="status"         />
        <result property="createBy"       column="create_by"      />
        <result property="createTime"     column="create_time"    />
        <result property="updateBy"       column="update_by"      />
        <result property="updateTime"     column="update_time"    />
        <result property="remark"         column="remark"         />
    </resultMap>

    <select id="selectCommentsByExperienceId" resultMap="PortalInterviewCommentResult">
        select id, experience_id, user_id, parent_id, reply_to_user_id, content, like_count, sort, status, create_by, create_time, update_by, update_time, remark
        from portal_interview_comment
        where experience_id = #{experienceId}
        and parent_id is null
        order by create_time desc
    </select>

    <select id="selectCommentsByParentId" resultMap="PortalInterviewCommentResult">
        select id, experience_id, user_id, parent_id, reply_to_user_id, content, like_count, sort, status, create_by, create_time, update_by, update_time, remark
        from portal_interview_comment
        where parent_id = #{parentId}
        order by create_time asc
    </select>

    <select id="countByExperienceId" resultType="long">
        select count(*) from portal_interview_comment where experience_id = #{experienceId}
    </select>

</mapper>
