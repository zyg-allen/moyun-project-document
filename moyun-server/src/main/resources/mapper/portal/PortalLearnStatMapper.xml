<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.moyun.portal.mapper.PortalLearnStatMapper">

    <select id="countSubmissionsByUser" resultType="Long">
        SELECT COUNT(*) FROM portal_interview_submission WHERE user_id = #{userId}
    </select>

    <select id="countSuccessByUser" resultType="Long">
        SELECT COUNT(*) FROM portal_interview_submission WHERE user_id = #{userId} AND is_success = 1
    </select>

    <select id="countTodaySubmissionsByUser" resultType="Long">
        SELECT COUNT(*) FROM portal_interview_submission WHERE user_id = #{userId} AND DATE(create_time) = CURDATE()
    </select>

    <select id="selectRecentActiveDates" resultType="java.time.LocalDate">
        SELECT DISTINCT DATE(create_time) FROM portal_interview_submission WHERE user_id = #{userId} AND create_time >= DATE_SUB(CURDATE(), INTERVAL #{days} DAY) ORDER BY DATE(create_time) DESC
    </select>

</mapper>
