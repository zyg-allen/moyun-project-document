<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.moyun.portal.mapper.PortalLikeMapper">

    <!--
        说明：原 PortalLikeController / IPortalLikeService / PortalLikeServiceImpl 已删除。
        文章点赞由 PortalArticleController.toggleLikeArticle 直接使用 BaseMapper 通用方法
        （selectOne / insert / deleteById / selectCount）操作 portal_like 表，
        不依赖本 XML 的自定义 SQL，故全部自定义语句已清理。
        保留 namespace 以支撑 MyBatis-Plus Mapper Bean 注册。
    -->

</mapper>
