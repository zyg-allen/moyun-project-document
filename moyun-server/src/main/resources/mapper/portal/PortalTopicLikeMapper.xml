<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.moyun.portal.mapper.PortalTopicLikeMapper">

    <select id="selectByTopicAndUser" resultType="com.moyun.portal.domain.entity.PortalTopicLike">
        SELECT * FROM portal_topic_like WHERE topic_id = #{topicId} AND user_id = #{userId} LIMIT 1
    </select>

    <delete id="deleteByTopicAndUser">
        DELETE FROM portal_topic_like WHERE topic_id = #{topicId} AND user_id = #{userId}
    </delete>

</mapper>
