<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.moyun.portal.mapper.PortalUserResumeMapper">

    <select id="selectListByUserId" resultType="com.moyun.portal.domain.entity.PortalUserResume">
        SELECT * FROM portal_user_resume WHERE user_id = #{userId} AND status &lt;> 'archived' ORDER BY update_time DESC
    </select>

    <select id="selectVersionHistory" resultType="com.moyun.portal.domain.entity.PortalUserResume">
        SELECT * FROM portal_user_resume WHERE (id = #{id} OR parent_id = #{id}) ORDER BY version_no ASC
    </select>

    <select id="countByUserId" resultType="int">
        SELECT COUNT(*) FROM portal_user_resume WHERE user_id = #{userId} AND status &lt;> 'archived'
    </select>

</mapper>
