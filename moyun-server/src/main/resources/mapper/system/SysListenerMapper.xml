<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.moyun.system.mapper.SysListenerMapper">

    <resultMap type="com.moyun.system.domain.entity.SysListener" id="SysListenerResult">
        <id     property="id"           column="id"             />
        <result property="name"         column="name"           />
        <result property="type"         column="type"           />
        <result property="eventType"    column="event_type"     />
        <result property="valueType"    column="value_type"     />
        <result property="value"        column="value"          />
        <result property="status"       column="status"         />
        <result property="createBy"     column="create_by"      />
        <result property="createTime"   column="create_time"    />
        <result property="updateBy"     column="update_by"      />
        <result property="updateTime"   column="update_time"    />
        <result property="remark"       column="remark"         />
    </resultMap>

    <sql id="selectSysListenerVo">
        select id, name, type, event_type, value_type, value, status, create_by, create_time, update_by, update_time, remark
        from sys_listener
    </sql>

    <select id="selectSysListenerList" parameterType="com.moyun.system.domain.entity.SysListener" resultMap="SysListenerResult">
        <include refid="selectSysListenerVo"/>
        <where>
            <if test="name != null and name != ''">
                AND name like concat('%', #{name}, '%')
            </if>
            <if test="type != null and type != ''">
                AND type = #{type}
            </if>
            <if test="eventType != null and eventType != ''">
                AND event_type = #{eventType}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        order by create_time desc
    </select>

</mapper>