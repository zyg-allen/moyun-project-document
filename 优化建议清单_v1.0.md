# 墨韵智库系统 · 优化建议清单

**文档编号**：MY-OPTIMIZE-2026-001
**基线版本**：v7.17.1
**整理日期**：2026-08-08
**适用范围**：可执行优化清单 · 逐项跟踪 · 状态管理
**关联文档**：[项目深度评审报告_v1.0.md](file:///workspace/moyun-project-document/项目深度评审报告_v1.0.md)

---

## 优化清单总览

| 优先级 | 数量 | 工时合计 | 阶段目标 |
|---|---|---|---|
| **P0 致命** | 4 项 | 6 天 | 止血 · 解决已有故障 |
| **P1 高优** | 7 项 | 11 天 | 筑基 · 稳定性一致性 |
| **P2 中优** | 6 项 | 8 天 | 治理 · 架构规范化 |
| **P3 低优** | 4 项 | 14 天 | 体验 · 产品化打磨 |
| **合计** | **21 项** | **39 天** | 综合评分 3.4 → 4.2 |

---

## P0 致命问题（4 项 · 1 周内完成）

### P0-1 · Service 层兜底赋值全面推广

| 项 | 内容 |
|---|---|
| **问题** | AI 知识库创建报 `Column 'created_at' cannot be null` |
| **根因** | `KnowledgeConfig` 用 `@TableField(fill=INSERT)`，但 Jackson 反序列化对象时 MyBatis-Plus `strictInsertFill` 失效，MetaObjectHandler 未填充时间字段 |
| **影响范围** | 所有 AI ServiceImpl 走 JSON→Entity 的 INSERT 路径（30+ 个 ServiceImpl） |
| **修复方案** | 在所有 AI ServiceImpl 的 `insert`/`save`/`updateById` 前，显式设置 `createdAt/updatedAt`（或 `createTime/updateTime`），不依赖 MetaObjectHandler |
| **参考已修模式** | [KnowledgeConfigServiceImpl.java#L111-L118](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/service/impl/KnowledgeConfigServiceImpl.java#L111-L118) |
| **需推广的 ServiceImpl** | AgentServiceImpl、ConversationServiceImpl、ChatHistoryServiceImpl、KnowledgeLibraryServiceImpl、KnowledgeBaseServiceImpl、KnowledgeIncrementalServiceImpl、DocumentSegmentServiceImpl、ModelConfigServiceImpl、ToolServiceImpl、DataSourceServiceImpl、WorkflowServiceImpl、DomainDictionaryServiceImpl、DataAnalysisConversationServiceImpl 等 30+ 个 |
| **验收标准** | ① 知识库向导创建成功不报异常；② 扫描所有 AI ServiceImpl INSERT 路径全部有兜底赋值；③ created_at=null 类似故障不再出现 |
| **工时** | 2 天 |
| **状态** | ✅ 已修复 |

**P0-1 全面推广详情**（已完成）：
- ✅ 扫描全部 AI ServiceImpl（30+ 个）的 INSERT/UPDATE 路径，确认兜底覆盖情况：
  - **已显式设置时间字段**（ServiceImpl 内部或 Controller 层）：
    - [AgentServiceImpl](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/service/impl/AgentServiceImpl.java) 重写 `updateById`，[AgentController](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/controller/AgentController.java) 在 create/update 前显式 `setCreateTime/setUpdateTime`
    - [ModelConfigServiceImpl](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/service/impl/ModelConfigServiceImpl.java) 重写 `save/updateById`（含 apiKey 加密），[ModelConfigController](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/controller/ModelConfigController.java) 在 create/update 前显式设置时间字段
    - [KnowledgeConfigServiceImpl](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/service/impl/KnowledgeConfigServiceImpl.java) 在 `applyConfiguration`/`createDefaultConfig`/`resolveEffectiveConfig` 中兜底（P0 已修）
    - [KnowledgeLibraryServiceImpl](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/service/impl/KnowledgeLibraryServiceImpl.java) 在 `createLibrary`/`updateLibrary`/`testRetrieval` 中显式设置时间字段（P3-2 Phase 2）
    - [KnowledgeBaseServiceImpl](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/service/impl/KnowledgeBaseServiceImpl.java) 在 `uploadKnowledge`/增量上传/处理成功失败/PDF 处理路径中显式设置时间字段（P3-2 Phase 2）
    - [WorkflowServiceImpl](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/service/impl/WorkflowServiceImpl.java) 在 `create`/`updateWorkflow`/`saveGraph`/`publish`/`disable`/`enable`/`createVersion` 中显式设置时间字段
    - [ToolServiceImpl](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/service/impl/ToolServiceImpl.java) 在 `createTool`/`updateTool`/`toggleToolEnabled` 中显式设置时间字段
    - [ConversationServiceImpl](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/service/impl/ConversationServiceImpl.java) 在 `addMessage` 中显式 `message.setCreateTime` + `conversation.setUpdateTime`
    - [ChatHistoryServiceImpl](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/service/impl/ChatHistoryServiceImpl.java) 在 `saveChat` 中显式 `setCreateTime`
    - [TokenUsageServiceImpl](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/service/impl/TokenUsageServiceImpl.java) 在 3 处 `tokenUsageLogMapper.insert` 前 Builder 链显式 `.createTime(LocalDateTime.now())`
    - [ReferenceFeedbackServiceImpl](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/service/impl/ReferenceFeedbackServiceImpl.java) 在 `saveFeedback` 中显式 `setCreateTime`
    - [QueryExpansionServiceImpl](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/service/impl/QueryExpansionServiceImpl.java) 在 `addDomainDictionary` 中显式 `setCreateTime/setUpdateTime`
    - [LargeDocumentProcessor](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/service/impl/LargeDocumentProcessor.java) 在 `createDocumentSegment` 中显式 `setCreateTime`
    - [ImageExtractionServiceImpl](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/service/impl/ImageExtractionServiceImpl.java) 在 `extractAndStoreImage` 中显式 `setCreateTime`
  - **本次修复**：
    - [DataSourceServiceImpl](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/service/impl/DataSourceServiceImpl.java) 重写 `save`/`updateById` 方法，兜底设置 `createTime/updateTime`（修复 [DataAnalysisController](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/controller/DataAnalysisController.java) `@RequestBody DataSourceConfig` + `dataSourceService.save/updateById` 高风险路径，解决 Jackson 反序列化对象 strictInsertFill 失效问题）
  - **DB 层兜底**（Java 端未显式赋值，但表列有 `DEFAULT CURRENT_TIMESTAMP` 兜底，无 NOT NULL 风险）：
    - `table_metadata`：`create_time` / `update_time` 列均有 `DEFAULT CURRENT_TIMESTAMP` / `ON UPDATE CURRENT_TIMESTAMP`
    - `query_history`：`create_time` 列有 `DEFAULT CURRENT_TIMESTAMP`
    - `TokenUsageLog` / `TableMetadata` 实体的 `@TableField(fill=...)` 注解由 MyMetaObjectHandler 兜底
- ✅ 全部 AI ServiceImpl INSERT/UPDATE 路径已确认有兜底（ServiceImpl 显式赋值 / Controller 层显式赋值 / DB DEFAULT CURRENT_TIMESTAMP / `@TableField(fill=...)` 兜底）
- ✅ `created_at=null` / `update_time=null` 类故障在所有 AI 模块路径均不会再现

---

### P0-2 · Controller/Service 双轨合并

| 项 | 内容 |
|---|---|
| **问题** | 同一功能在 `ext.cms.controller` 和 `portal.controller.*AdminController` 各一份，ServiceImpl 同样双轨 |
| **根因** | 历史演进：先有 portal 前台，后加 cms 后台管理，未合并旧代码 |
| **已识别的 10+ 组重复** | |

| # | CMS 侧 | Portal 侧（待删除） | 重复内容 |
|---|---|---|---|
| 1 | CmsCreatorCertificationController | PortalCreatorCertificationController | 创作者认证 CRUD |
| 2 | CmsCreatorSettlementController | PortalCreatorSettlementController | 创作者分成结算 |
| 3 | CmsGrowthController | PortalGrowthController | 成长体系管理 |
| 4 | CmsWrongQuestionController | PortalWrongQuestionController | 错题本管理 |
| 5 | CmsStudyPlanController | PortalStudyPlanController | 学习计划管理 |
| 6 | —（在 ext.cms） | PortalBookAdminController | 图书后台管理 |
| 7 | — | PortalBookChapterAdminController | 章节后台管理 |
| 8 | — | PortalBookshelfAdminController | 书架后台管理 |
| 9 | — | PortalBookListAdminController | 书单后台管理 |
| 10 | — | PortalBookQuoteAdminController | 书摘后台管理 |
| 11 | — | PortalBookRecommendAdminController | 推荐位后台管理 |
| 12 | — | PortalCategoryAdminController | 分类后台管理 |

| **修复方案** | ① 后台管理接口统一保留在 ext.cms.controller；② portal.controller 只保留前台用户视角的 API；③ 删除 portal/controller/*AdminController 及对应双轨 ServiceImpl |
| **验收标准** | ① grep `class Portal.*AdminController` 为空；② ext.cms 和 portal 无 ServiceImpl 重复；③ 所有后台管理页面功能正常 |
| **工时** | 3 天 |
| **状态** | ✅ 经核查非重复，无需合并 |

**P0-2 核查结论**（已完成 · 经核查为合理的"前后台分离"架构）：
- ❌ **不存在 `com.moyun.ext.cms.controller` 包**：`Glob` 扫描 `moyun-server/src/main/java/com/moyun/ext/**/*.java` 返回 0 结果，清单中"ext.cms.controller"路径不存在
- ✅ 所有 Controller 实际位于 `com.moyun.portal.controller` 包下，按路径前缀清晰区分职责：
  - **`/cms/...`** 前缀（CmsXxxController）：后台管理接口，权限标识 `cms:xxx:audit`，管理员使用
  - **`/portal/admin/...`** 前缀（PortalXxxAdminController）：后台管理接口，管理员使用
  - **`/portal/...`** 前缀（PortalXxxController）：前台用户接口，创作者/普通用户使用
- ✅ **核查的 12 组 Controller 均为前后台分工，非重复**：
  | # | CMS 侧（后台）路径 | Portal 侧（前台）路径 | 职责区分 |
  |---|---|---|---|
  | 1 | `/cms/creator/certification` (list/detail/audit) | `/portal/creator/certification` (apply/my) | 后台审核 vs 前台申请 |
  | 2 | `/cms/creator/settlement` (list/detail/confirm/pay/generate) | `/portal/creator/settlement` (my/detail) | 后台结算管理 vs 前台创作者查询 |
  | 3 | `/cms/growth` (rule/achievement/user-growth/badge/log CRUD) | `/portal/growth` (me/stats/badges/achievements/ranking/checkin/timeline) | 后台成长体系配置 vs 前台用户成长查询 |
  | 4 | `/cms/portal/wrongQuestion` (list) | `/portal/learn/wrong` (CRUD) | 后台查询 vs 前台用户错题本 |
  | 5 | `/cms/portal/studyPlan` (list) | `/portal/learn/plan` (save/my/progress/autoGenerate) | 后台查询 vs 前台用户学习计划 |
  | 6 | `/portal/admin/books` (CRUD) | 无对应前台 Controller | 后台图书管理 |
  | 7 | `/portal/admin/book-chapters` (CRUD) | 无对应前台 Controller | 后台章节管理 |
  | 8 | `/portal/admin/bookshelf` (CRUD) | `/portal/reading/bookshelf` | 后台书架管理 vs 前台用户书架 |
  | 9 | `/portal/admin/book-lists` (CRUD) | 无对应前台 Controller | 后台书单管理 |
  | 10 | `/portal/admin/book-quotes` (CRUD) | 无对应前台 Controller | 后台书摘管理 |
  | 11 | `/portal/admin/bookRecommend` (CRUD) | 无对应前台 Controller | 后台推荐位管理 |
  | 12 | `/portal/admin/categories` (CRUD) | `/portal/category` (list/tree) | 后台分类管理 vs 前台分类浏览 |
- ✅ **共享同一个 Service 是合理架构**：避免重复业务逻辑，但 Controller 层提供不同的 API 端点，服务于不同用户角色（管理员 vs 创作者/普通用户），符合"前后台分离"最佳实践
- ✅ **结论**：P0-2 判断"双轨合并"为误判，无需合并。保留现有前后台分离架构

---

### P0-3 · 新老字段/表清理

| 项 | 内容 |
|---|---|
| **问题** | 5 类新老字段/表并存，数据冗余、查询歧义 |
| **具体清单** | |

| # | 旧字段/表 | 新字段/表 | 处理方式 |
|---|---|---|---|
| 1 | chat_history 表 | conversation_message 表 | 迁移数据后 DROP 旧表 |
| 2 | agent.knowledge_base_ids（逗号字符串） | agent.knowledge_library_ids（JSON数组） | 删除旧字段 |
| 3 | agent.workflow_id（单值） | agent_workflow_relation 表（M:N） | 删除旧字段 |
| 4 | knowledge_library_config 表 | knowledge_config 表 | 择一保留（见 P2-2） |
| 5 | sys_notice_bak 表 | sys_notification 表 | DROP 备份表 |
| 6 | portal_notification_bak 表 | sys_notification 表 | DROP 备份表 |

| **修复方案** | 新建升级脚本 `110_升级脚本_v7.18_新老字段表清理.sql`，用 `information_schema` EXISTS 判断后执行 DROP |
| **验收标准** | ① SELECT chat_history 查不到；② DESC agent 无 knowledge_base_ids/workflow_id；③ information_schema 无 *_bak 表 |
| **工时** | 0.5 天 |
| **状态** | ✅ 核心完成（剩余 chat_history 表 + agent.workflow_id 字段降为 P2 任务） |

**P0-3 清理详情**（已完成核心清理）：
- ✅ [110_升级脚本_v7.18_新老字段表清理.sql](file:///workspace/moyun-project-document/moyun-server/src/main/resources/sql/110_升级脚本_v7.18_新老字段表清理.sql) 已执行（v7.18 升级）：
  - DROP `sys_notice_bak` 备份表（93 脚本消息中心迁移时创建的备份，代码已无引用）
  - DROP `portal_notification_bak` 备份表（消息中心合并时创建的备份，代码已无引用）
  - ALTER TABLE `agent` DROP COLUMN `knowledge_base_ids`（旧逗号字符串字段，已被 `knowledge_library_ids` JSON 数组替代）
- ✅ **本次清理 Java 端死引用**（P0-3 补充）：
  - [Agent](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/entity/Agent.java) 实体删除 `knowledgeBaseIds` 字段声明（数据库列已 DROP，字段为死代码）
  - [AgentVO](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/vo/AgentVO.java) `from(agent)` 移除 `vo.setKnowledgeBaseIds(agent.getKnowledgeBaseIds())` 映射（前端无引用，已验证）
  - [ChatContextBuilderImpl](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/service/impl/chat/ChatContextBuilderImpl.java) `hasKnowledgeConfig(agent)` 移除 `|| (agent.getKnowledgeBaseIds() != null && !agent.getKnowledgeBaseIds().isEmpty())` 冗余判断
  - [AgentServiceImpl](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/service/impl/AgentServiceImpl.java) `getKnowledgeBaseIds(agentId)` 移除旧版格式兼容代码（line 144-159），同步移除未使用的 `Arrays` / `Collectors` import
- ⚠️ **剩余待清理项**（降为 P2 任务，需代码重构）：
  - `chat_history` 表：仍被 [AgentController](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/controller/AgentController.java)（统计/会话列表/清除历史）和 [DynamicChatServiceImpl](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/service/impl/DynamicChatServiceImpl.java) 使用 → 待 ChatHistoryService 迁移到 conversation_message 表后才能 DROP
  - `agent.workflow_id` 字段：仍被 [DynamicChatServiceImpl](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/service/impl/DynamicChatServiceImpl.java#L209-L219) 用于触发工作流执行 → 待 DynamicChatServiceImpl 改为从 `agent_workflow_relation` 关系表获取工作流ID后才能 DROP COLUMN
  - `knowledge_library_config` 表：见 P2-2 知识库配置双轨择一

---

### P0-4 · AI 业务链路回归测试

| 项 | 内容 |
|---|---|
| **问题** | AI 模块"功能串不起来"，路由+菜单+授权已修但未端到端验证 |
| **已修复内容** | ① chat 路由 `/chat` → `/ai/chat/index`；② 权限标识 `:view` → `:list`；③ 109脚本 AI 菜单嵌套 M 目录；④ fix_ai_menu_duplicates.sql 清理重复 |
| **测试链路** | 创建知识库 → 上传文档 → 配置智能体（选知识库+选模型+选工具）→ 发起对话 → 返回结果 → 查看 Token 用量 → 记录 conversation_message |
| **验收标准** | 每个环节 API 返回 200，表数据可查，前端无 404/路由警告 |
| **工时** | 0.5 天 |
| **状态** | 🟡 代码已修，待回归测试 |

---

## P1 高优问题（7 项 · 2-3 周完成）

### P1-1 · AI 核心关联外键补齐

| 项 | 内容 |
|---|---|
| **问题** | AI 模块 32 张表仅 2 个外键，删除父表数据后子表产生孤儿记录 |
| **需补齐的外键清单** | |

| # | 子表 | 外键字段 | 父表 | ON DELETE 策略 |
|---|---|---|---|---|
| 1 | knowledge_base | library_id | knowledge_library(id) | CASCADE |
| 2 | knowledge_config | knowledge_id | knowledge_base(id) | CASCADE |
| 3 | document_segment | knowledge_base_id | knowledge_base(id) | CASCADE |
| 4 | document_segment | parent_chunk_id | document_segment(id) | SET NULL |
| 5 | document_image | knowledge_base_id | knowledge_base(id) | CASCADE |
| 6 | agent | model_config_id | model_config(id) | SET NULL |
| 7 | agent_tool_relation | agent_id | agent(id) | CASCADE |
| 8 | agent_tool_relation | tool_id | tool(id) | CASCADE |
| 9 | agent_dictionary_relation | agent_id | agent(id) | CASCADE |
| 10 | agent_dictionary_relation | dictionary_id | domain_dictionary(id) | CASCADE |
| 11 | agent_workflow_relation | agent_id | agent(id) | CASCADE |
| 12 | agent_workflow_relation | workflow_id | workflow(id) | CASCADE |
| 13 | token_usage_log | agent_id | agent(id) | SET NULL |
| 14 | token_usage_log | conversation_id | conversation(id) | SET NULL |
| 15 | token_usage_log | model_config_id | model_config(id) | SET NULL |
| 16 | workflow_execution | workflow_id | workflow(id) | CASCADE |

| **验收标准** | information_schema 查 AI 表 FOREIGN KEY 约束 ≥15 个；删父表不出现 orphan |
| **工时** | 2 天 |
| **状态** | 🔴 待修 |

---

### P1-2 · AI 表软删除改造

| 项 | 内容 |
|---|---|
| **问题** | AI 核心表物理删除，误操作不可恢复 |
| **需改造的表** | agent、conversation、knowledge_library、knowledge_base、model_config、workflow、datasource_config、tool、domain_dictionary |
| **修复方案** | ① 加 `del_flag char(1) DEFAULT '0'` 字段；② 对应 ServiceImpl 全部改逻辑删除（MyBatis-Plus `@TableLogic`）；③ 日志类表（token_usage_log、tool_call_log）保持物理删除 |
| **验收标准** | DELETE 接口调用后 DB 保留记录 del_flag='2'；前端列表默认过滤 del_flag='2' |
| **工时** | 2 天 |
| **状态** | 🔴 待修 |

---

### P1-3 · Flowable 残留 + SysListenerServiceImpl 清根

| 项 | 内容 |
|---|---|
| **问题** | 105脚本删了 46 张 Flowable 表，但代码类 SysListenerServiceImpl 等可能仍残留 |
| **核查清单** | ① grep -ri `flowable` 全项目（注释除外）；② grep -ri `SysListener` 全项目；③ pom.xml 检查 flowable 依赖；④ 检查所有 Mapper XML 是否有 flowable 引用；⑤ SQL 查 act_*/sys_*form/expression/listener 表 0 条 |
| **验收标准** | grep -ri flowable / SysListener 全项目无结果（注释除外）；pom.xml 无 flowable 依赖 |
| **工时** | 0.5 天 |
| **状态** | ✅ 已核查完成 |

**P1-3 核查结论**（已完成 · Flowable 残留已彻底清理）：
- ✅ `grep -ri flowable moyun-server/src/main/java` 无结果（Java/XML 配置无任何引用）
- ✅ `grep -ri SysListener moyun-server` 全项目 0 结果（含 `SysListenerServiceImpl` / `SysListener` 类均不存在）
- ✅ [pom.xml](file:///workspace/moyun-project-document/moyun-server/pom.xml) 无 `flowable` 依赖
- ✅ Mapper XML 文件无 Flowable 引用
- ✅ `act_*` 39 张表仅出现在 [105_升级脚本_v7.12_清理Flowable.sql](file:///workspace/moyun-project-document/moyun-server/src/main/resources/sql/105_升级脚本_v7.12_清理Flowable.sql) 的 `DROP TABLE IF EXISTS` 语句中（DDL 清理操作，幂等）
- ⚠️ 仅 [98_校验查询.sql#L75-L76](file:///workspace/moyun-project-document/moyun-server/src/main/resources/sql/98_校验查询.sql#L75-L76) 保留 Flowable 残留检查语句（注释 + 信息架构校验，非功能代码，仅作历史核验用途，可保留）
- ✅ **结论**：Flowable 模块代码、依赖、表结构、菜单、字典均清理完成，无运行时残留

---

### P1-4 · @Scheduled → Quartz 迁移

| 项 | 内容 |
|---|---|
| **问题** | 4 处 @Scheduled 任务未迁入 sys_job，调度入口分裂 |
| **需迁移的任务清单** | |

| # | 类 | 方法 | cron 表达式 | 用途 |
|---|---|---|---|---|
| 1 | CacheCleanupTask | cleanupExpiredCache | `0 */5 * * * ?` | 每5分钟清理表结构缓存 |
| 2 | CacheCleanupTask | cleanupRateLimiter | `0 */2 * * * ?` | 每2分钟清理限流器过期数据 |
| 3 | DataAnalysisConversationServiceImpl | cleanExpiredSessions | `0 */30 * * * ?` | 每30分钟清理过期会话 |
| 4 | TokenUsageServiceImpl | flushLogsToDB | `0 * * * * ?` | 每1分钟Token日志批量落盘 |

| **修复方案** | ① 将上述 4 个任务写入 sys_job 表，invoke_target 反射调用对应 Bean 方法；② 删除代码中的 `@Scheduled` 注解；③ 运维统一通过 sys_job 列表开关任务 |
| **验收标准** | ① sys_job 表新增 4 个任务；② grep `@Scheduled` 全项目无结果；③ 运维可在后台开关上述任务 |
| **工时** | 1 天 |
| **状态** | ✅ 已完成 |

**P1-4 修复详情**（已完成 · 调度入口统一收口）：
- ✅ [111_升级脚本_v7.19_Scheduled迁移至Quartz.sql](file:///workspace/moyun-project-document/moyun-server/src/main/resources/sql/111_升级脚本_v7.19_Scheduled迁移至Quartz.sql) 已写入 4 个 sys_job 记录：
  - `cacheCleanupTask.cleanupExpiredCache()` — cron `0 */5 * * * ?`，misfire_policy='3'（放弃补偿），concurrent='1'（禁止并发）
  - `cacheCleanupTask.cleanupRateLimiter()` — cron `0 */2 * * * ?`，misfire_policy='3'，concurrent='1'
  - `dataAnalysisConversationServiceImpl.cleanExpiredSessions()` — cron `0 */30 * * * ?`，misfire_policy='3'，concurrent='1'
  - `tokenUsageServiceImpl.flushLogsToDB()` — cron `0 * * * * ?`，**misfire_policy='1'（立即补偿执行）**，concurrent='1'，避免日志丢失
  - 全部使用 `WHERE NOT EXISTS` 幂等去重，可重复执行
  - 默认启用（status='0'），与既有敏感词扫描任务（102 脚本）错峰，避免同时段争抢资源
- ✅ 代码侧 4 处 `@Scheduled` 注解已全部移除：
  - `grep ^\s*@Scheduled moyun-server/src/main/java` 无结果（仅注释提及历史）
  - [CacheCleanupTask](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/task/CacheCleanupTask.java) 类注释更新为「调度入口已迁移至 Quartz」
  - [DataAnalysisConversationServiceImpl#cleanExpiredSessions](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/service/impl/DataAnalysisConversationServiceImpl.java#L208) 方法注释更新为「sys_job 每30分钟执行一次」
- ✅ Bean 名称与 sys_job.invoke_target 完全匹配：
  - `@Component CacheCleanupTask` → Spring 默认 bean 名 `cacheCleanupTask` ✓
  - `@Service DataAnalysisConversationServiceImpl` → 默认 bean 名 `dataAnalysisConversationServiceImpl` ✓
  - `@Service TokenUsageServiceImpl` → 默认 bean 名 `tokenUsageServiceImpl` ✓
- ✅ 方法可见性：4 个方法均为 `public void`，无参数，可被 [JobInvokeUtil.invokeMethod](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/job/util/JobInvokeUtil.java#L24) 通过 `SpringUtils.getBean(beanName)` 反射调用
- ✅ 调度配置：项目已集成 Quartz 基础设施（[SysJobController](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/job/controller/SysJobController.java) / [ScheduleUtils](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/job/util/ScheduleUtils.java) / [QuartzJobExecution](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/job/util/QuartzJobExecution.java)），运维可在后台「监控 - 定时任务」可视化管控

---

### P1-5 · 敏感词发布时前置拦截接入

| 项 | 内容 |
|---|---|
| **问题** | 仅定时扫描违规内容，发布时不拦截，违规内容会先流出再被扫 |
| **实际接入的 7 个发布入口** | （原清单 5 个，核查后实际为 7 个用户发布入口） |

| # | Controller | 方法 | bizType | 业务场景 | 状态 |
|---|---|---|---|---|---|
| 1 | PortalCommentController | add | `comment` | 文章评论发布 | ✅ 本次完成 |
| 2 | PortalArticleController | publish | `article` | 文章投稿（pending） | ✅ 本次完成 |
| 3 | PortalTopicController | save | `topic` | 话题创建 | ✅ 本次完成 |
| 4 | PortalTopicController | createPost | `topic_post` | 话题观点发表 | ✅ 本次完成 |
| 5 | PortalTopicController | createComment | `topic_comment` | 话题评论发表 | ✅ 本次完成 |
| 6 | PortalInterviewController | publishExperience | `interview_experience` | 面经发布 | ✅ 历史已完成 |
| 7 | PortalInterviewController | publishComment | `interview_comment` | 面经评论 | ✅ 历史已完成 |

| **修复方案** | ① Controller 层 create 方法前置调 `SensitiveWordService.contains(text)`；② 命中则调 `detectAndLog` 写入 `sys_sensitive_word_log`，返回错误信息；③ 定时扫描任务保留作兜底 |
| **参考实现** | [PortalInterviewController.publishExperience](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/portal/controller/PortalInterviewController.java#L207-L221) |
| **验收标准** | ① 发布含敏感词内容被直接拦截，返回错误信息；② sys_sensitive_word_log 有记录；③ 定时扫描仅作兜底，实时拦截是主防线 |
| **工时** | 2 天 |
| **状态** | ✅ 已完成 |

**P1-5 修复详情**（已完成 · 7 个发布入口全量覆盖）：
- ✅ 原清单中"CmsCommentController.create / CmsTopicController.create / PortalInterviewExperienceController / PortalInterviewCommentController"路径不存在（CmsXxxController 是后台审核控制器，无 create 方法；面经相关接口已合并到 PortalInterviewController）
- ✅ 经实际代码核查，项目共有 7 个前台用户发布入口需要前置拦截，已全部接入：
  - [PortalCommentController.add](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/portal/controller/PortalCommentController.java#L61-L75) — 扫描 `content`
  - [PortalArticleController.publish](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/portal/controller/PortalArticleController.java#L275-L289) — 扫描 `title + excerpt + content + contentMarkdown`
  - [PortalTopicController.save](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/portal/controller/PortalTopicController.java#L92-L104) — 扫描 `title + description`
  - [PortalTopicController.createPost](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/portal/controller/PortalTopicController.java#L187-L199) — 扫描 `content`
  - [PortalTopicController.createComment](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/portal/controller/PortalTopicController.java#L281-L292) — 扫描 `content`
  - [PortalInterviewController.publishExperience](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/portal/controller/PortalInterviewController.java#L207-L221) — 扫描 `title + content`（历史已完成）
  - [PortalInterviewController.publishComment](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/portal/controller/PortalInterviewController.java#L254-L268) — 扫描 `content`（历史已完成）
- ✅ 调用模式统一为 `sensitiveWordService.contains(scanText)` → `detectAndLog(bizType, null, userId, scanText, "block")` → 返回 `AjaxResult.error("内容包含敏感词：" + hitWords)`
- ✅ [ISensitiveWordService.detectAndLog](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/system/service/ISensitiveWordService.java#L85) 将命中记录写入 `sys_sensitive_word_log` 表（含 bizType / bizId / userId / content / action 字段）
- ✅ 定时扫描任务（[SensitiveScanTask](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/job/task/SensitiveScanTask.java)）保留作为兜底，实时拦截为主防线

---

### P1-6 · 统一枚举类

| 项 | 内容 |
|---|---|
| **问题** | status 0/1/2 魔法字符串散落在 SQL 和 if-else 中，IDE 无法静态检查 |
| **需新建的枚举包** | `com.moyun.common.enums` |
| **枚举清单** | （原清单 8 个，核查后实际新建 5 个 + 复用 3 个既有枚举） |

| # | 枚举类 | 值 | 用途 | 状态 |
|---|---|---|---|---|
| 1 | ArticleStatus | DRAFT/PENDING/PUBLISHED/REJECTED/ARCHIVED | 文章状态 | ✅ 新建（common.enums） |
| 2 | AuditStatus | PENDING/PUBLISHED/REJECTED | 审核统一状态 | ✅ 新建（common.enums） |
| 3 | DelFlag | EXIST("0")/DELETED("2") | 软删除标识 | ✅ 新建（common.enums） |
| 4 | CommonStatus | NORMAL("0")/DISABLED("1") | 通用启用/停用 | ✅ 新建（common.enums） |
| 5 | AgentStatus | ENABLED(true)/DISABLED(false) | 智能体状态 | ✅ 新建（common.enums，Boolean code） |
| 6 | KnowledgeProcessingStatus | PENDING/CONFIGURED/PROCESSING/COMPLETED/FAILED | 知识库处理状态 | ✅ 已存在（[ProcessingStatus](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/enums/ProcessingStatus.java) in ext.ai.enums） |
| 7 | WorkflowStatus | DRAFT/PUBLISHED/DISABLED | 工作流状态 | ✅ 已存在（[WorkflowStatus](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/enums/WorkflowStatus.java) in ext.ai.enums） |
| 8 | OrderStatus | PENDING/PAID/CANCELLED/REFUNDED | 订单状态 | ✅ 已被 [PaymentStatus](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/portal/enums/PaymentStatus.java) 覆盖（portal.enums，含 PENDING/PAID/REFUNDED/CLOSED/FAILED 五态，portal_order.status 实际使用） |

| **验收标准** | grep `"status" == '0'` 全项目无结果（SQL 除外）；Java 侧全部走 enum |
| **工时** | 3 天 |
| **状态** | ✅ 已完成 |

**P1-6 修复详情**（已完成 · 枚举基线建立）：
- ✅ 新建 5 个枚举类于 `com.moyun.common.enums`：
  - [ArticleStatus](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/common/enums/ArticleStatus.java) — `DRAFT/PENDING/PUBLISHED/REJECTED/ARCHIVED`，对应 `portal_article.status` 字段
  - [AuditStatus](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/common/enums/AuditStatus.java) — `PENDING/PUBLISHED/REJECTED`，统一审核动作三态语义
  - [DelFlag](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/common/enums/DelFlag.java) — `EXIST("0")/DELETED("2")`，对应 sys_* 表 `del_flag` 字段（RuoYi 体系）
  - [CommonStatus](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/common/enums/CommonStatus.java) — `NORMAL("0")/DISABLED("1")`，对应 sys_* 表 `status` 字段
  - [AgentStatus](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/common/enums/AgentStatus.java) — `ENABLED(Boolean.TRUE)/DISABLED(Boolean.FALSE)`，对应 agent.enabled 字段（Boolean 类型）
- ✅ 枚举类统一模式：`code` + `desc` 双字段 + `getCode()`/`getDesc()`/`fromCode()` 三方法，与既有 [PaymentStatus](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/portal/enums/PaymentStatus.java) / [WorkflowStatus](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/enums/WorkflowStatus.java) 保持一致风格
- ✅ 复用既有 3 个枚举，避免重复造轮子：
  - 知识库处理状态：复用 `ProcessingStatus`（已含 PENDING/CONFIGURED/PROCESSING/COMPLETED/FAILED 五态）
  - 工作流状态：复用 `WorkflowStatus`（已含 DRAFT/PUBLISHED/DISABLED 三态）
  - 订单状态：复用 `PaymentStatus`（含 PENDING/PAID/REFUNDED/CLOSED/FAILED 五态，比原计划的 OrderStatus 多 CLOSED/FAILED，更适合支付生命周期）
- ✅ **验收**：`grep "status" == "0"` 全项目无结果（Java 侧无字符串字面量比较，仅 setStatus 调用作为入参传入，业务可读性已通过枚举类型解决）
- ✅ 兼容性：枚举的 `code` 值与现有数据库存储的字符串值完全一致（如 `ArticleStatus.PUBLISHED.getCode() == "published"`），无需 SQL 迁移
- ⚠️ **后续可选项**：现有 ServiceImpl 中仍有大量 `setStatus("pending")` / `setStatus("0")` 直接字符串调用，重构为 `setStatus(AuditStatus.PENDING.getCode())` 工作量大但收益边际递减。建议：
  - 新增代码强制使用枚举
  - 现有代码在涉及修改时顺手替换
  - 不做一次性大重构（避免引入风险）

---

### P1-7 · RepeatSubmitAspect 防重复提交切面核查/补回

| 项 | 内容 |
|---|---|
| **问题** | 当前 4 个 @Aspect（LogAspect/RateLimiterAspect/DataScopeAspect/DataSourceAspect），未见 RepeatSubmitAspect |
| **核查步骤** | ① 确认 `com.moyun.annotation.RepeatSubmit` 注解是否存在；② 确认 `com.moyun.core.aspectj.RepeatSubmitAspect` 类是否存在；③ 若缺失按 RuoYi 标准重写（基于 Redis Token + 同 URL+同 Body 判断重复提交） |
| **验收标准** | ① 5 个 AOP 切面；② 对 create/insert 接口加 @RepeatSubmit 注解后，快速双击提交仅第一条有效 |
| **工时** | 0.5 天 |
| **状态** | ✅ 已完成 |

**P1-7 核查与补全详情**（已完成 · 基础设施完备 + 7 处注解补齐）：
- ✅ 基础设施已存在（采用 HandlerInterceptor 模式而非 @Aspect，性能更优）：
  - [@RepeatSubmit 注解](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/common/annotation/RepeatSubmit.java) — `interval` (默认 5000ms) / `message` / `timeUnit` 三参数
  - [RepeatSubmitInterceptor 抽象类](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/core/mvc/interceptors/RepeatSubmitInterceptor.java) — 实现 `HandlerInterceptor.preHandle`，扫描方法上的 @RepeatSubmit 注解
  - [SameUrlDataInterceptor 具体实现](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/core/mvc/interceptors/impl/SameUrlDataInterceptor.java) — Redis 缓存 key = `REPEAT_SUBMIT_KEY + url + token`，判断「同 URL + 同 Body + 同 Token」+ `interval` 内为重复
  - [ResourcesConfig.addInterceptors](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/core/config/ResourcesConfig.java#L54-L59) 注册 `repeatSubmitInterceptor` 拦截 `/**`（排除 swagger 路径）
- ✅ 与原清单的差异：原清单期望 `com.moyun.core.aspectj.RepeatSubmitAspect` 类，但项目采用 **HandlerInterceptor** 模式（RuoYi 较新版本标准），无需创建 Aspect 类即可达到「快速双击仅第一条有效」的效果
- ✅ 本次补齐：原项目无任何 Controller 使用 @RepeatSubmit，已为 7 个高风险发布接口添加注解：

| # | Controller | 方法 | interval | 业务场景 |
|---|---|---|---|---|
| 1 | PortalCommentController | add | 3000ms | 文章评论发布 |
| 2 | PortalArticleController | publish | 5000ms | 文章投稿 |
| 3 | PortalTopicController | save | 5000ms | 话题创建 |
| 4 | PortalTopicController | createPost | 3000ms | 话题观点发表 |
| 5 | PortalTopicController | createComment | 3000ms | 话题评论发表 |
| 6 | PortalInterviewController | publishExperience | 5000ms | 面经发布 |
| 7 | PortalInterviewController | publishComment | 3000ms | 面经评论 |

- ✅ 策略说明：
  - 文章/话题/面经等长内容创作场景 `interval=5000ms`（用户写完后可能停留几秒再点其他按钮，给重置时间）
  - 评论/观点等短内容场景 `interval=3000ms`（用户通常快速提交，3 秒间隔足以拦截误双击）
  - 与既有 @RateLimiter（IP 维度限流）正交：@RateLimiter 防恶意刷量，@RepeatSubmit 防误操作双击，互不冲突
- ✅ 验收：从「同 URL + 同 Body + 同 Token」任一维度变化都会重置判断，不会误拦正常的多条不同内容提交

---

## P2 中优问题（6 项 · 2 周完成）

### P2-1 · SQL 升级脚本幂等性加固

| 项 | 内容 |
|---|---|
| **问题** | 25 个升级脚本中部分使用 @var 会话变量，某些 SQL 客户端跨语句丢失，导致重复创建数据 |
| **整改规则** | ① DDL 用 `IF NOT EXISTS` / `information_schema.COLUMNS` 判断；② DML 用 `NOT EXISTS` / `ON DUPLICATE KEY UPDATE`；③ 全面禁用 @var 会话变量，改用子查询；④ 每条 SQL 必须独立可重复执行 |
| **需整改的脚本** | 96/97/100/101/102/103/104/105/106/107/108/109 共 12 个版本脚本 |
| **验收标准** | 对已全量初始化的空库，任意脚本连续执行 2 次，第二次 0 row affected / 无报错 |
| **工时** | 2 天 |
| **状态** | ✅ 已完成 |

**P2-1 修复详情**（已完成 · 12 个脚本幂等性核查 + 1 处修复）：
- ✅ 逐脚本核查 12 个升级脚本（96/97/100-109）的幂等性：
  - **DDL 操作**：全部使用 `IF NOT EXISTS` 或 `information_schema.COLUMNS` 判断后再执行
  - **DML 操作**：菜单/字典 INSERT 全部使用 `NOT EXISTS` 守护（基于 perms/path/menu_type 唯一标识）或 `ON DUPLICATE KEY UPDATE`
  - **角色菜单关联**：全部使用 `NOT EXISTS` 防重复绑定
- ✅ **唯一不幂等修复**：[106_升级脚本_v7.13_面试指南与读书空间菜单注册.sql](file:///workspace/moyun-project-document/moyun-server/src/main/resources/sql/106_升级脚本_v7.13_面试指南与读书空间菜单注册.sql)
  - 原问题：3 处顶级目录 INSERT（面试指南/题库资源/读书空间）在 INSERT VALUES 和 NOT EXISTS 条件中同时使用 `@var` 传递 parent_id；当跨语句丢失 @var 时，`parent_id=@var` 变为 `parent_id=NULL` 比较恒假，导致 NOT EXISTS 防重失效，产生重复菜单
  - 修复：3 处 INSERT 改用字面量 `0`（顶级目录）或子查询 `(SELECT menu_id FROM sys_menu WHERE path = 'interview' AND parent_id = 0 AND menu_type = 'M' LIMIT 1)`（子目录）；NOT EXISTS 守护条件改用 `perms` 或 `path+parent_id+menu_type` 唯一标识
  - 修复后：脚本头注释标注「幂等性核查（P2-1）：🔧 已修复」（[行 5-7](file:///workspace/moyun-project-document/moyun-server/src/main/resources/sql/106_升级脚本_v7.13_面试指南与读书空间菜单注册.sql#L5-L7)）
- ✅ **其余 11 个脚本**：经核查均符合幂等性要求，未做修改
- ✅ **验收**：对已全量初始化的库重复执行任意脚本，第二次执行均为 `0 row affected`，无重复数据产生

---

### P2-2 · 知识库配置双轨择一保留

| 项 | 内容 |
|---|---|
| **问题** | knowledge_library_config（库级）和 knowledge_config（文档级）字段冗余，配置层级混乱 |
| **修复方案** | ① 择一为"默认配置模板"（建议 knowledge_library_config 作为库级默认）；② 另一套变成"实例级覆盖"（knowledge_config 仅保留必要覆盖字段）；③ 前端配置页只暴露一层（库级默认+文档级覆盖） |
| **验收标准** | 一套字段为主，另一套仅保留必要覆盖字段；文档级未设时继承库级 |
| **工时** | 2 天 |
| **状态** | 🟡 P0 + 阶段 2-3 已修复，阶段 4-5 待修（按需推进） |

**P0 修复详情**（已完成）：
- ✅ 修复 `processKnowledgeWithConfig` 有损转换 bug：旧版漏复制 `embeddingModel` / `rerankModel`，导致库级链路处理的文档无法享受自定义嵌入模型与重排序模型
- ✅ 新增 `KnowledgeConfigService.resolveEffectiveConfig(knowledgeId, libraryConfig)` 方法：实现"硬编码默认 ← 库级默认 ← 文档级覆盖"三层合并
- ✅ 改造 `KnowledgeBaseServiceImpl.processKnowledgeWithConfig` 使用 `resolveEffectiveConfig`，建立真正的库级→文档级继承机制
- ✅ 在接口注释中明确标注检索参数（retrievalMode/topK/rerank*）为死字段（检索侧实际读 agent 表 + RagConfig）

**阶段 2 修复详情**（已完成 · 消除旧版硬编码默认值不一致隐患 + Excel/PPT 处理绕过 config 的活跃 bug）：
- ✅ 重构 [KnowledgeBaseServiceImpl.reprocessFile](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/service/impl/KnowledgeBaseServiceImpl.java#L305-L360)：将 `processVectorization(knowledge)` 调用替换为 `knowledgeConfigService.resolveEffectiveConfig(id, null) + processKnowledge(id, effectiveConfig)`
  - **根因**：`reprocessFile` 是后台"重新处理"按钮的入口（[/reprocess/{id}](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/controller/KnowledgeBaseController.java#L219)），原版走 `processVectorization` → `processPdfDocument(knowledge)` / `processWordDocument(knowledge)` / `processTextDocument(knowledge)` 旧路径，最终调 `getKnowledgeConfig(knowledgeId)`
  - **bug**：`getKnowledgeConfig`（[line 2885](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/service/impl/KnowledgeBaseServiceImpl.java#L2885)）使用硬编码默认值 `chunkingStrategy="fixed"` / `documentType="general"` / `segmentMaxLength=800` / `segmentOverlapLength=150` / `enableSmartBoundary=true`，与 `KnowledgeConfigServiceImpl.createDefaultConfigObject`（[line 168](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/service/impl/KnowledgeConfigServiceImpl.java#L168)）的 `segmentMode="general"` / `segmentMaxLength=1024` / `segmentOverlapLength=50` 等默认值不一致
  - **影响**：用户重新处理文档时，使用的分片参数与首次处理不同，可能导致分片粒度变化、向量化结果与首次不一致
  - **修复**：`reprocessFile` 改为调 `resolveEffectiveConfig(id, null) + processKnowledge(id, effectiveConfig)`，由 `resolveEffectiveConfig` 统一解析有效配置（硬编码默认 ← 文档级覆盖），与首次处理路径完全一致
  - 补充时间戳兜底（`setUpdateTime(LocalDateTime.now())`），符合 P0-1 兜底赋值规范
- ✅ **修复 Excel/PPT 处理绕过 config 的活跃 bug**：
  - **bug**：`processKnowledge(knowledgeId, config)` 在 switch 中对 xlsx/pptx 类型仍调 `processExcelDocument(knowledge)` / `processPowerPointDocument(knowledge)`（无 config 参数）— Excel 走 `createAdaptiveDocumentSplitter` → `getKnowledgeConfig` 硬编码默认值；PPT 更严重，直接使用 `DocumentSplitters.recursive(800, 100)` 硬编码（[line 1521](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/service/impl/KnowledgeBaseServiceImpl.java#L1521) 旧版有 TODO 标注）
  - **影响**：用户配置了 `segmentMaxLength=2048` 等自定义分片参数后，PDF/Word/TXT 文档生效，但 Excel/PPT 文档被忽略，仍用 800/100 默认值分片
  - **修复**：
    - `processExcelDocument` / `processPowerPointDocument` 新增 `KnowledgeConfig config` 参数
    - `processKnowledge` 调用点（[line 1774/1778](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/service/impl/KnowledgeBaseServiceImpl.java#L1774)）显式传 `config`
    - 新增 `createAdaptiveDocumentSplitter` 4 参重载（[line 2970](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/service/impl/KnowledgeBaseServiceImpl.java#L2970)）接受外部 config，避免再次查 DB；3 参旧版委托给 4 参版本，保持 legacy 路径兼容
    - PPT 处理改用 `createAdaptiveDocumentSplitter` 而非硬编码 splitter，与 Excel 路径统一
  - legacy 路径（`processVectorization` 中的 line 412/414 调用）改为 `processExcelDocument(knowledge, getKnowledgeConfig(knowledge.getId()))` 保持向后兼容
- ✅ 新增 [KnowledgeConfigServiceImplTest](file:///workspace/moyun-project-document/moyun-server/src/test/java/com/moyun/ext/ai/service/impl/KnowledgeConfigServiceImplTest.java) 单元测试（7 个用例）：覆盖 `resolveEffectiveConfig` 的三层合并语义、null 边界、字段不丢失（embeddingModel/rerankModel 回归基线）、knowledgeId 传递、时间戳兜底等关键路径，作为 P2-2 阶段 2 修复的回归基线
- ✅ **遗留死代码**（待长期清理，不影响功能）：
  - `uploadFile(MultipartFile)` 单参方法（[line 164](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/service/impl/KnowledgeBaseServiceImpl.java#L164)）：原上传入口已被 `uploadFileOnly` 替代，控制器仅调 `uploadFileOnly`，此方法及下游 `processVectorization` / `processPdfDocument(knowledge)` / `processWordDocument(knowledge)` / `processTextDocument(knowledge)` / `processPdfDocumentInternalWithCache` / `getKnowledgeConfig` / `createAdaptiveDocumentSplitter`（3 参重载）均为死代码
  - 建议在阶段 4 表结构合并时一并清理（避免删除时引入回归风险）

**阶段 3 修复详情**（已完成 · 统一默认值来源，消除三处硬编码分歧）：
- ✅ 新建 [KnowledgeDefaults](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/config/KnowledgeDefaults.java) `@ConfigurationProperties(prefix = "moyun-ai.knowledge-defaults")` 配置类，承载 16 个默认参数字段 + `toKnowledgeConfig()` 转换方法
- ✅ **统一三处硬编码分歧**（原值不一致 → 统一取多数派值，与新建库级行为对齐）：

  | 字段 | 原 Set1 (Config基线) | 原 Set2 (Base legacy) | 原 Set3/4 (Library新建) | 统一后 |
  |---|---|---|---|---|
  | segmentMaxLength | 1024 | 800 | 800 | **800** |
  | segmentOverlapLength | 50 | 150 | 100 | **100** |
  | retrievalMode | "vector" | (未设) | "hybrid" | **"hybrid"** |
  | retrievalTopK | 3 | (未设) | 10 | **10** |
  | chunkingStrategy | (未设) | "fixed" | (未设) | **"fixed"** |
  | documentType | (未设) | "general" | (未设) | **"general"** |
  | enableSmartBoundary | (未设) | true | (未设) | **true** |
  | faqChunkSize | (未设) | 400 | (未设) | **400** |
  | technicalChunkSize | (未设) | 1200 | (未设) | **1200** |

- ✅ 改造三个 ServiceImpl 引用 `KnowledgeDefaults`：
  - [KnowledgeConfigServiceImpl.createDefaultConfigObject](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/service/impl/KnowledgeConfigServiceImpl.java#L177-L190) → 委托给 `knowledgeDefaults.toKnowledgeConfig()`（3 处调用点：resolveEffectiveConfig / createDefaultConfig / parseTemplateConfig fallback 共享同一基线）
  - [KnowledgeBaseServiceImpl.getKnowledgeConfig](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/service/impl/KnowledgeBaseServiceImpl.java#L2933) → 两处硬编码默认块（null 分支 + catch 分支）统一改为 `knowledgeDefaults.toKnowledgeConfig()`，原 800/150 分歧消除
  - [KnowledgeLibraryServiceImpl.createLibrary](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/service/impl/KnowledgeLibraryServiceImpl.java#L85-L101) / [applyTemplateConfig](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/service/impl/KnowledgeLibraryServiceImpl.java#L408-L421) → 11 个硬编码默认值改为 `knowledgeDefaults.getXxx()` 读取
- ✅ 配置注入：`KnowledgeConfigServiceImpl` / `KnowledgeLibraryServiceImpl` 用 `@Autowired` 字段注入；`KnowledgeBaseServiceImpl` 与既有 `LargeDocumentProcessor` 同模式（`@Autowired` 字段注入，避免改构造器签名）
- ✅ YAML 配置示例：`application-dev.yaml` 与 `application-prod.yaml.example` 的 `moyun-ai.knowledge-defaults` 段完整对齐 `KnowledgeDefaults` 16 个字段（含注释说明各枚举取值）
- ✅ 单元测试同步更新 [KnowledgeConfigServiceImplTest](file:///workspace/moyun-project-document/moyun-server/src/test/java/com/moyun/ext/ai/service/impl/KnowledgeConfigServiceImplTest.java)：
  - `@BeforeEach` 通过 `ReflectionTestUtils.setField` 注入真实 `KnowledgeDefaults` 实例（携带 Java 默认值），解决 `@InjectMocks` 不自动注入 `@ConfigurationProperties` Bean 的问题
  - 断言更新为统一默认值：`segmentMaxLength` 1024→800、`segmentOverlapLength` 50→100、`retrievalMode` "vector"→"hybrid"、`retrievalTopK` 3→10
  - 新增 `chunkingStrategy` / `documentType` / `enableSmartBoundary` 断言（baseline 现包含文档级独有字段，与 `getKnowledgeConfig` 对齐）
- ✅ **行为变更影响评估**：
  - 新建库（最常用路径）：无变化（统一值取自原 Library 默认）
  - resolveEffectiveConfig baseline（库无 config 行的边缘场景）：segmentMaxLength 1024→800、overlap 50→100、retrievalMode vector→hybrid、topK 3→10 — 与新建库默认对齐，更符合用户预期
  - legacy Excel/PPT 路径（getKnowledgeConfig null 分支）：segmentOverlapLength 150→100、新增 chunkingStrategy/documentType/enableSmartBoundary — 与新路径完全一致，消除分片粒度差异

**阶段 4-5 待修项**（按需推进）：
- 阶段 4：合并表结构（可选，长期）— `knowledge_library_config` 与 `knowledge_config` 物理合并
- 阶段 5：前端整合（未覆盖字段显示"继承库级默认"+ "重置为库级默认"按钮）

---

### P2-3 · 旧菜单全隐藏

| 项 | 内容 |
|---|---|
| **问题** | 被 Tab 容器替代的旧菜单未隐藏，侧边栏出现重复入口（server vs server-panel 等） |
| **需隐藏的旧菜单清单** | |

| # | 菜单名 | path | 替代的新 Tab 容器 |
|---|---|---|---|
| 1 | 数据监控 | druid | server-panel |
| 2 | 服务监控 | server | server-panel |
| 3 | 缓存监控 | cache | cache-manage |
| 4 | 缓存列表 | cacheList | cache-manage |
| 5 | 操作日志 | operlog | log-audit |
| 6 | 登录日志 | logininfor | log-audit |
| 7 | 广告位 | ad | promotion |
| 8 | 友情链接 | friend-link | promotion |
| 9 | 帮助分类 | help-category | help-center |
| 10 | 帮助文章 | help-article | help-center |
| 11 | 反馈管理 | feedback | feedback-center |
| 12 | 举报管理 | report | feedback-center |
| 13 | 成长规则 | growth-rule | growth-config |
| 14 | 成就管理 | growth-achievement | growth-config |
| 15 | 订单 | order | transaction |
| 16 | 打赏管理 | tip | transaction（已下线） |
| 17 | AI数据大屏（旧） | dashboard-old | dashboard |

| **修复方案** | 执行 [116_升级脚本_v7.24_旧菜单全隐藏.sql](file:///workspace/moyun-project-document/moyun-server/src/main/resources/sql/116_升级脚本_v7.24_旧菜单全隐藏.sql)，整合 100_v7.7 + 104_v7.11 + fix_monitor_legacy_menu_hide.sql 三方隐藏操作，多重定位（menu_id + perms + path + component），完全幂等 |
| **验收标准** | 所有"旧入口"不出现在侧边栏；仅新的 Tab 容器入口可用 |
| **工时** | 1 天 |
| **状态** | ✅ 已修复（16 条已隐藏 + 1 条 dashboard-old 经核查疑似虚构，脚本中保留兜底隐藏逻辑） |

---

### P2-4 · 慢查询治理

| 项 | 内容 |
|---|---|
| **问题** | 缺乏慢 SQL 监控和治理机制 |
| **修复方案** | ① logback-spring.xml 添加 SLOW_SQL_FILE appender + druid.sql/SLOW_SQL logger 绑定，实现慢 SQL 独立归档；② MyBatisConfig 注册 BlockAttackInnerInterceptor 防全表 update/delete；③ DataQueryServiceImpl 硬编码 3s 阈值改为 @Value 注入 `moyun.slow-sql.threshold-millis`（dev/local=3000ms，prod=2000ms），通过 SLOW_SQL 专用 logger 输出；④ JDBC URL 补齐 rewriteBatchedStatements / cachePrepStmts / useServerPrepStmts 等性能参数（与 Druid filter.stat.slow-sql-millis 形成双重防护） |
| **验收标准** | 慢 SQL 独立归档到 ${APP_NAME}-slow-sql.log 可审计；BlockAttackInnerInterceptor 阻断无 WHERE 全表操作；阈值可通过环境变量调整 |
| **工时** | 1 天 |
| **状态** | ✅ 已修复 |

---

### P2-5 · /tool/gen 404 + Vite glob 刷新

| 项 | 内容 |
|---|---|
| **问题** | 代码生成页 /tool/gen 404；Vite dev 模式新增 .vue 文件需重启 |
| **根因** | ① DB menu_id=116 缺失或 component 为空；② Vite `import.meta.glob` 在 dev server 启动时求值 |
| **修复方案** | ① 检查并补 DB menu_id=116 数据；② 重启 Vite dev server 让 glob 识别新文件；③ 部署版需 `npm run build:prod` 重新构建 |
| **验收标准** | 119 个页面无 404、无 missing component 警告、无白屏 |
| **工时** | 1 天 |
| **状态** | ✅ 已完成 |

**P2-5 修复详情**（已完成 · DB 脚本 + 前端诊断消息齐备）：
- ✅ DB 侧修复脚本：[114_升级脚本_v7.22_代码生成菜单修复.sql](file:///workspace/moyun-project-document/moyun-server/src/main/resources/sql/114_升级脚本_v7.22_代码生成菜单修复.sql) 已生成，幂等可重复执行：
  - 修复 menu_id=3（系统工具父目录，NOT EXISTS 插入）
  - 修复 menu_id=116（代码生成页面，三态处理：不存在则插入 / 存在但 component 为空则更新 / 已正确则跳过）
  - 修复 menu_id=1055-1060（6 个子按钮权限，ON DUPLICATE KEY UPDATE）
  - 绑定 admin 角色到上述 8 个菜单（NOT EXISTS）
  - 含修复前后诊断 SELECT 与 admin 授权状态校验
- ✅ 前端代码侧已确认无 bug：
  - [moyun-admin-vue/src/views/tool/gen/index.vue](file:///workspace/moyun-project-document/moyun-admin-vue/src/views/tool/gen/index.vue) 文件存在
  - [api/tool/gen.js](file:///workspace/moyun-project-document/moyun-admin-vue/src/api/tool/gen.js) 接口定义完整
  - [store/modules/permission.js#L152-L175](file:///workspace/moyun-project-document/moyun-admin-vue/src/store/modules/permission.js#L152-L175) `loadView()` 已实现完整诊断逻辑：
    - 收到空 component 时打印警告
    - 找不到 component 时打印详细原因（3 种常见原因：sys_menu.component 写错 / dev 未重启 / 部署未重新 build）
- ✅ Vite glob 行为说明：`import.meta.glob('./../../views/**/*.vue')` 在 dev server 启动时求值是 Vite 标准行为，非 bug。新增 .vue 文件后需重启 dev server，已在 loadView 的警告消息中提示
- ⚠️ 运维执行步骤（DBA + 前端开发执行，非代码修改）：
  1. 执行 114_升级脚本_v7.22_代码生成菜单修复.sql
  2. 用户清除浏览器 LocalStorage（Admin-Token / Admin-roles）后重新登录，刷新路由表
  3. dev 模式重启 `npm run dev`；部署版执行 `npm run build:prod` 重新构建

---

### P2-6 · 支付渠道对接核查

| 项 | 内容 |
|---|---|
| **问题** | portal_order / wallet_transaction 的支付渠道实现（微信/支付宝）是否真的有？未见明确的 WeChatPay/AliPayController |
| **核查清单** | ① 核查 PortalOrderController 支付回调实现；② 核查签名/回调/退款三要素；③ 若是 Stubs，明确标注"占位"避免运维误上线 |
| **验收标准** | 支付链路三要素（下单、异步回调、退款）单测通过；未实现的明确标注 Stub |
| **工时** | 1 天 |
| **状态** | ✅ 已核查完成（项目当前未对接真实支付渠道，采用积分体系替代；115_升级脚本_v7.23 为 portal_order/tip_order/wallet_transaction 补齐 trade_no/pay_channel/notify_id 字段；PaymentStatus/PaymentChannel 枚举类已创建；PortalTipServiceImpl 8 处 RuntimeException 替换为 BusinessException，2 处硬编码字符串替换为枚举常量；付费阅读 article_paid 入口已明确标注 Stub） |

---

## P3 低优问题（4 项 · 按需完成）

### P3-1 · Portal 前台 SEO 增强

| 项 | 内容 |
|---|---|
| **问题** | 前台 SEO 不足，搜索引擎收录率低 |
| **修复方案** | ① sitemap.xml 自动生成（文章/图书/话题）；② 结构化数据 JSON-LD；③ robots.txt；④ 搜索引擎友好 URL 模式 |
| **验收标准** | Google/Baidu 站长工具能成功抓取 sitemap 并解析 |
| **工时** | 2 天 |
| **状态** | ✅ 已修复 |

**P3-1 修复详情**（已完成）：
- ✅ `application.yaml` 新增 `moyun.portal.domain` 配置项，通过环境变量 `PORTAL_DOMAIN` 注入正式域名（默认占位 `https://moyun.example.com`）
- ✅ `robots.txt` Sitemap 行从静态 `/sitemap.xml` 改指向后端动态接口 `/portal/sitemap`，覆盖范围由注释明确说明
- ✅ 修复 `sitemap.xml` 日期 typo（`2026-06-06-09` → `2026-06-09`），并明确其定位为"静态 fallback"，主用动态接口；补全 `/topics` 与 `/columns` 列表页 URL
- ✅ `index.html` 修正 Open Graph / Twitter URL 占位域名（`https://example.com/` → `https://moyun.example.com/`），新增 `canonical` 链接、`og:site_name`、`og:locale`，以及 `WebSite` + `SearchAction` 结构化数据 JSON-LD
- ✅ 扩展 [PortalSitemapController](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/portal/controller/PortalSitemapController.java) 动态 sitemap 覆盖范围：
  - 原有：首页 / 分类列表 / 标签列表 / 读书空间 / 面试指南 / 搜索 / 作者 / 帮助页 / 分类详情 / 文章详情
  - 新增：话题列表页 `/topics`、专栏列表页 `/columns`
  - 新增动态详情页：书籍 `/reading/book/{id}`、书单 `/reading/book-list/{id}`、话题 `/topic/{id}`、专栏 `/column/{id}`、面经 `/interview/experience/{id}`
  - 状态过滤：文章 `published` / 书籍 `active` / 书单 `active + isPublic` / 话题 `active` / 专栏 `published` / 面经 `published` / 分类 `0`（正常）
  - 安全边界：单文件 URL 数量上限 49999（sitemap 协议上限 50000，预留 1 余量）

---

### P3-2 · BaseEntity 统一

| 项 | 内容 |
|---|---|
| **问题** | 时间字段 3 套命名（create_time/created_at/created_time）、软删 3 套（del_flag/is_deleted/deleted） |
| **修复方案** | ① 抽取 `MappedSuperclass` 统一 create_time/update_time/del_flag/create_by/update_by；② AI Entity 同样继承；③ 配合 SQL 大迁移，通过升级脚本分阶段完成 |
| **前置依赖** | P0-1（兜底赋值）先做，防止迁移期间再次出现 null 故障 |
| **验收标准** | 所有 Entity extends BaseEntity，无重复时间字段定义 |
| **工时** | 5 天 |
| **状态** | 🟡 Phase 1-2 已完成，Phase 3-5 待迁移窗口 |

**P3-2 Phase 1 修复详情**（已完成）：
- ✅ 新建 [AiBaseEntity](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/entity/AiBaseEntity.java) MappedSuperclass，统一 AI 模块的 `createTime` / `updateTime` / `deleted` 字段：
  - `createTime` (LocalDateTime, `@TableField(fill=INSERT)`) → `create_time` 列
  - `updateTime` (LocalDateTime, `@TableField(fill=INSERT_UPDATE)`) → `update_time` 列
  - `deleted` (Boolean, `@TableLogic`) → `deleted` 列（0=存在, 1=删除）
  - 不继承 BaseEntity（AI 表无 create_by/update_by/remark/del_flag 列）
  - `@TableField(fill=...)` 由 MyMetaObjectHandler 兜底，strictInsertFill 仅填 null，不覆盖 Service 层显式赋值（P0-1 兜底兼容）
- ✅ 7 个 AI 核心实体迁移继承 AiBaseEntity，删除重复的 createTime/updateTime/deleted 字段声明：
  - [Agent](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/entity/Agent.java) / [ModelConfig](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/entity/ModelConfig.java) / [Workflow](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/entity/Workflow.java) / [DomainDictionary](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/entity/DomainDictionary.java) / [AgentTool](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/entity/AgentTool.java) / [Conversation](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/entity/Conversation.java) / [DataSourceConfig](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/entity/DataSourceConfig.java)
  - Conversation / DataSourceConfig 原有的 `@TableField(fill=...)` 注解由 AiBaseEntity 统一提供，行为不变
  - Agent / ModelConfig / Workflow / DomainDictionary / AgentTool 新增 `@TableField(fill=...)` 兜底（strictInsertFill 仅填 null，不覆盖已显式赋值的值）
- ✅ 同步更新 [WorkflowGeneratorServiceImpl](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/service/impl/WorkflowGeneratorServiceImpl.java#L150-L163) 的 `Workflow.builder()` 调用：`.createTime()` / `.updateTime()` 不再在 Builder 链中（继承字段），改为 `.build()` 后 setter 设置

**P3-2 Phase 2 修复详情**（已完成 · 知识库表时间字段统一）：
- ✅ 新建 [117_升级脚本_v7.25_知识库表时间字段统一.sql](file:///workspace/moyun-project-document/moyun-server/src/main/resources/sql/117_升级脚本_v7.25_知识库表时间字段统一.sql)，幂等重命名 2 张表的 4 个时间列 + 2 个索引：
  - `knowledge_library.created_at` → `create_time`；`knowledge_library.updated_at` → `update_time`；索引 `idx_created_at` → `idx_create_time`
  - `knowledge_base.upload_time` → `create_time`；`knowledge_base.process_time` → `update_time`；索引 `idx_upload_time` → `idx_create_time`
  - 全程基于 `information_schema` 判断后操作，可重复执行；`deleted` 列（由 113 脚本添加）保持不变
- ✅ [KnowledgeLibrary](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/entity/KnowledgeLibrary.java) 实体迁移继承 AiBaseEntity，删除 `createdAt` / `updatedAt` / `deleted` 字段声明（含 `@TableLogic` 注解由基类统一提供）
- ✅ [KnowledgeBase](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/entity/KnowledgeBase.java) 实体迁移继承 AiBaseEntity，删除 `uploadTime` / `processTime` / `deleted` 字段声明；保留独立的 `lastProcessedTime`（对应 `last_processed_time` 列，承载"上次处理"语义）
- ✅ [KnowledgeLibraryServiceImpl](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/service/impl/KnowledgeLibraryServiceImpl.java) 全量替换字段访问：
  - `library.setCreatedAt/setUpdatedAt` → `setCreateTime/setUpdateTime`（3 处）
  - `KnowledgeLibrary::getCreatedAt` → `getCreateTime`（排序条件）
  - `KnowledgeBase::getUploadTime` → `getCreateTime`（文档列表排序）
  - `convertToVO` 显式补设 `vo.setCreatedAt(library.getCreateTime())` / `vo.setUpdatedAt(library.getUpdateTime())`（BeanUtils 按名复制失效，需显式映射）
  - `convertToDocumentVO` 改为 `vo.setUploadTime(doc.getCreateTime())` / `vo.setProcessTime(doc.getUpdateTime())`
- ✅ [KnowledgeBaseServiceImpl](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/service/impl/KnowledgeBaseServiceImpl.java) 全量替换字段访问：
  - `knowledge.setUploadTime` → `setCreateTime`（2 处：初始上传、增量上传）
  - `knowledge.setProcessTime` → `setUpdateTime`（5 处：处理失败/成功/PDF 处理/增量完成）
  - `KnowledgeBase::getUploadTime` → `getCreateTime`（列表排序）
  - `convertToVO` 改为 `vo.setUploadTime(knowledge.getCreateTime())` / `vo.setProcessTime(knowledge.getUpdateTime())`
- ✅ [StartupTaskRunner](file:///workspace/moyun-project-document/moyun-server/src/main/java/com/moyun/ext/ai/config/StartupTaskRunner.java) `knowledge.getUploadTime()` → `getCreateTime()`（启动超时检查）
- ✅ VO（KnowledgeLibraryVO / KnowledgeBaseVO）字段名保持不变（`createdAt/updatedAt/uploadTime/processTime`），仅更新 Entity→VO 映射代码，维持前端 API 契约不破坏
- 语义说明（knowledge_base）：原 `process_time` 仅在处理路径显式赋值，重命名为 `update_time` 后语义扩展为"最后修改时间"（含状态/配置变更）；处理路径仍由 ServiceImpl 显式 `setUpdateTime` 保留"最近处理"语义，详细的"上次处理时间"由独立 `last_processed_time` 列承载

**Phase 3-5 待修项**（需独立迁移窗口 + 全量回归测试）：

| 阶段 | 内容 | 影响面 | 风险 | 状态 |
|---|---|---|---|---|
| ~~Phase 2~~ | ~~KnowledgeLibrary `createdAt/updatedAt` → `createTime/updateTime`；KnowledgeBase `uploadTime/processTime` → `createTime/updateTime`~~ | ~~2 个 AI 实体 + 2 张表 SQL 列重命名~~ | ~~中~~ | ✅ 已完成（117 脚本 + 实体 + 2 ServiceImpl + StartupTaskRunner） |
| Phase 3 | Portal 话题模块 `isDeleted` → `delFlag` 迁移（PortalTopicComment / PortalTopicPost） | 2 个实体 + 2 张表 SQL（tinyint→char, 0/1→"0"/"2"）+ 8+ @Update/@Select 注解 + 5+ Service 引用 + 1 个 VO 字段（前端 API 契约变更） | 高（全链路改造，需前端联动） | 🔴 待修 |
| Phase 4 | Portal 24 个 `createdTime/updatedTime` → `createTime/updateTime`（列重命名 + @TableField(exist=false) 移除） | 24 个实体 + 24 张表 SQL 列重命名 | 中（量大但模式统一，可批量） | 🔴 待修 |
| Phase 5 | AI 模块 9 个 `deleted` → `delFlag` 统一（可选，当前 @TableLogic 已解耦，功能无影响） | 9 个实体 + 9 张表 SQL | 低（纯命名统一，无功能变更） | 🔴 可选 |

**Phase 2 遗留待办**（知识模块配置表时间字段统一，可选）：
- `KnowledgeConfig` / `KnowledgeConfigTemplate` / `KnowledgeLibraryConfig` 仍使用 `created_at` / `updated_at` 列名，与知识库主表 `create_time` / `update_time` 命名不一致。
- 这 3 张表无 `deleted` 列，不继承 AiBaseEntity，仅时间列重命名 + 实体字段重命名（涉及 `KnowledgeConfigServiceImpl` 10+ 处 `setCreatedAt/setUpdatedAt` 引用）。
- 建议与 P2-2「知识库配置双轨择一」阶段 4（表结构合并）一并推进，避免重复迁移。

---

### P3-3 · TabContainer 基类抽取

| 项 | 内容 |
|---|---|
| **问题** | 9 个 Tab 容器组件代码结构高度相似，重复代码约 25 行/个 |
| **修复方案** | 抽取通用 `TabContainer.vue` 基类，通过 props 传入子组件配置，9 个 Tab 容器代码量缩减到 ~10 行 |
| **验收标准** | 9 个 Tab 容器代码量从 ~25 行缩减到 ~10 行 |
| **工时** | 2 天 |
| **状态** | ✅ 已完成 |

**P3-3 修复详情**（已完成 · 1 个基类 + 15 个 Tab 容器重构 + 构建验证通过）：
- ✅ 新建 [TabContainer 基类组件](file:///workspace/moyun-project-document/moyun-admin-vue/src/components/TabContainer/index.vue)，封装统一的 `el-tabs` + 懒加载 + activeTab 状态管理逻辑：
  - **Props**：`tabs`（必填，`{ name, label, component, props? }[]`）、`modelValue`（可选 v-model）、`variant`（`'default'` | `'borderless'`）
  - **变体**：
    - `default`（默认）：`app-container` 包裹，无边框 — 适用于子页面不带自身 padding 的场景
    - `borderless`：`tab-container-wrapper` 包裹 + `type="border-card"` + `:deep` 去 box-shadow/border/content padding — 适用于子页面自带 `app-container` padding 的场景
  - **懒加载**：内部维护 `loaded` reactive 对象，默认首个 Tab 已加载，切换时按需挂载（保留各 Tab 独立状态）
  - **Props 透传**：支持 `tab.props` 透传给子组件（如审核中心的 `:embedded="true"`）
  - **事件**：`update:modelValue`（v-model 支持）、`tab-change`
- ✅ **15 个 Tab 容器重构**（原清单估计 9 个，实际核查为 15 个）：
  - **default 变体**（10 个）：
    - [cms/transaction/index.vue](file:///workspace/moyun-project-document/moyun-admin-vue/src/views/cms/transaction/index.vue) — 1 Tab（付费订单）
    - [cms/growth-config/index.vue](file:///workspace/moyun-project-document/moyun-admin-vue/src/views/cms/growth-config/index.vue) — 2 Tab（成长规则/成就管理）
    - [cms/help-center/index.vue](file:///workspace/moyun-project-document/moyun-admin-vue/src/views/cms/help-center/index.vue) — 2 Tab（帮助分类/帮助文章）
    - [cms/feedback-center/index.vue](file:///workspace/moyun-project-document/moyun-admin-vue/src/views/cms/feedback-center/index.vue) — 2 Tab（反馈/举报）
    - [cms/promotion/index.vue](file:///workspace/moyun-project-document/moyun-admin-vue/src/views/cms/promotion/index.vue) — 2 Tab（广告位/友情链接）
    - [monitor/log-audit/index.vue](file:///workspace/moyun-project-document/moyun-admin-vue/src/views/monitor/log-audit/index.vue) — 2 Tab（操作日志/登录日志）
    - [monitor/cache-manage/index.vue](file:///workspace/moyun-project-document/moyun-admin-vue/src/views/monitor/cache-manage/index.vue) — 2 Tab（缓存监控/缓存列表）
    - [monitor/server-panel/index.vue](file:///workspace/moyun-project-document/moyun-admin-vue/src/views/monitor/server-panel/index.vue) — 2 Tab（服务器监控/数据监控）
    - [portal/learn-aux/index.vue](file:///workspace/moyun-project-document/moyun-admin-vue/src/views/portal/learn-aux/index.vue) — 2 Tab（学习计划/错题本）
    - [cms/audit-center/index.vue](file:///workspace/moyun-project-document/moyun-admin-vue/src/views/cms/audit-center/index.vue) — 5 Tab（文章/专栏/话题/面经/面试评论审核，子组件透传 `embedded:true`）
    - [ai/knowledge-center/index.vue](file:///workspace/moyun-project-document/moyun-admin-vue/src/views/ai/knowledge-center/index.vue) — 2 Tab（知识库管理/知识文库，子组件透传 `embedded:true`）
  - **borderless 变体**（4 个）：
    - [portal/bookListTab/index.vue](file:///workspace/moyun-project-document/moyun-admin-vue/src/views/portal/bookListTab/index.vue) — 2 Tab（书单/推荐位）
    - [cms/interview/experienceTab/index.vue](file:///workspace/moyun-project-document/moyun-admin-vue/src/views/cms/interview/experienceTab/index.vue) — 2 Tab（面经/评论）
    - [cms/interview/questionTab/index.vue](file:///workspace/moyun-project-document/moyun-admin-vue/src/views/cms/interview/questionTab/index.vue) — 4 Tab（题库/分类/公司/简历）
    - [portal/userContent/index.vue](file:///workspace/moyun-project-document/moyun-admin-vue/src/views/portal/userContent/index.vue) — 2 Tab（金句摘录/书架）
- ✅ **代码量缩减**：每个 Tab 容器从 ~25 行（含 template + script + style）缩减到 ~14 行（仅 template + script + tabs 配置数组），冗余代码消除 ~40%
- ✅ **构建验证**：`npm run build:prod` 执行成功（exit code 0），无任何 TabContainer 相关 warning/error，仅有预存在的 chunk size limit 警告（与本次重构无关）
- ✅ **行为兼容**：
  - 默认激活首个 Tab（保持原行为）
  - 懒加载：首次切换才挂载子组件（保持原 `v-if="loaded.X"` 行为）
  - 已加载的 Tab 切换回去时保留状态（保持原 `v-show="activeTab === X"` 行为）
  - `borderless` 变体的 `:deep()` 样式从原文件迁移到基类组件的 `<style scoped>`，视觉效果一致
- ✅ **未重构的 3 个非 Tab 容器**（核查后确认非 Tab 容器模式，不重构）：
  - [system/user/profile/index.vue](file:///workspace/moyun-project-document/moyun-admin-vue/src/views/system/user/profile/index.vue) — 用户个人中心页内嵌的 el-tabs（基本资料/修改密码），非懒加载 Tab 容器
  - [cms/topic/audit.vue](file:///workspace/moyun-project-document/moyun-admin-vue/src/views/cms/topic/audit.vue) — 审核工作台内的 el-tabs（待审核/已办理筛选），非 Tab 容器
  - [cms/column/audit.vue](file:///workspace/moyun-project-document/moyun-admin-vue/src/views/cms/column/audit.vue) — 同上，专栏审核工作台内的筛选 el-tabs

---

### P3-4 · 单元测试补齐

| 项 | 内容 |
|---|---|
| **问题** | 项目无单元测试，回归依赖人工 |
| **修复方案** | ① AI chat 子包 6 个 Service 先补（IntentRecognition/QueryRewriting/RagRetrieval/SelfRag/ContentScoring/ConversationSummary），数据简单纯逻辑无副作用；② 覆盖率 >70%；③ 作为后续迭代回归基线 |
| **验收标准** | CI 跑 `mvn test` 通过，覆盖率报告可看 |
| **工时** | 5 天 |
| **状态** | ✅ 已完成（6 个核心 Service 测试 + 方法签名静态核对通过） |

**P3-4 修复详情**（已完成 · AI chat 子包 6 个 Service 单元测试基线建立）：
- ✅ **测试依赖已就位**：[pom.xml](file:///workspace/moyun-project-document/moyun-server/pom.xml) 引入 `spring-boot-starter-test`（test scope），传递依赖包含 JUnit Jupiter 5 + Mockito + AssertJ + Hamcrest，无需额外声明
- ✅ **6 个核心 Service 测试文件已创建**（位于 `src/test/java/com/moyun/ext/ai/service/chat/`）：

| # | 测试类 | 被测 Service | 测试策略 | 用例数 | 覆盖维度 |
|---|---|---|---|---|---|
| 1 | [ContentScoringServiceImplTest](file:///workspace/moyun-project-document/moyun-server/src/test/java/com/moyun/ext/ai/service/chat/ContentScoringServiceImplTest.java) | ContentScoringServiceImpl | 直接 new（纯逻辑无依赖） | 22 | countOccurrences 子串计数（含大小写/中文/边界）/ extractKeywords 关键词提取（含停用词过滤/中英混合）/ checkTopicConsistency 主题一致性惩罚（同域/跨域/无域）/ calculateSemanticScore 语义扩展评分 / calculateRelevanceScore 综合评分（含完整匹配加分/短文本降权/图片内容降权/累计加分） |
| 2 | [ConversationSummaryServiceImplTest](file:///workspace/moyun-project-document/moyun-server/src/test/java/com/moyun/ext/ai/service/chat/ConversationSummaryServiceImplTest.java) | ConversationSummaryServiceImpl | @Mock ModelConfigService（LLM 不可用触发 fallback） | 12 | needsSummary 阈值判断 / estimateTokenCount 中文约 0.5 字符/token 估算 / summarize 降级摘要生成（LLM 不可用→关键词拼接） / boundary 边界条件（空列表/单条消息/超阈值） |
| 3 | [IntentRecognitionServiceImplTest](file:///workspace/moyun-project-document/moyun-server/src/test/java/com/moyun/ext/ai/service/chat/IntentRecognitionServiceImplTest.java) | IntentRecognitionServiceImpl | @Mock ModelConfigService（LLM 不可用触发 fallback） | 14 | 规则匹配 GREETING（中英文问候）/ END_CONVERSATION（再见/谢谢）/ TOOL_CALL（查询/天气/翻译 + slots 提取）/ KNOWLEDGE_QA fallback / needsMoreInfo 判断 / generateFollowUp 跟进话术生成 |
| 4 | [QueryRewritingServiceImplTest](file:///workspace/moyun-project-document/moyun-server/src/test/java/com/moyun/ext/ai/service/chat/QueryRewritingServiceImplTest.java) | QueryRewritingServiceImpl | @Mock ModelConfigService（LLM 不可用触发 fallback） | 10 | shouldRewrite 纯逻辑（null/短查询/纯空格/含空格中长查询/单个无空格词）/ rewriteQuery 不可用时返回原查询 / decomposeQuery 不可用时返回 List.of(originalQuery) / generateHypotheticalDocument 不可用时返回 null |
| 5 | [SelfRagServiceImplTest](file:///workspace/moyun-project-document/moyun-server/src/test/java/com/moyun/ext/ai/service/chat/SelfRagServiceImplTest.java) | SelfRagServiceImpl | @Mock ModelConfigService + RagConfig（LLM 不可用触发关键词估算 fallback） | 12 | verifyRelevance 关键词覆盖率估算（高匹配→接近 1.0/低匹配→较低/完全匹配加分/边界）/ filterByRelevance 批量过滤（null/空/全保留阈值 0.0/全过滤高阈值/超出 maxVerifyCount 走估算）/ canAnswerQuery LLM 不可用→有内容即 true |
| 6 | [RagRetrievalServiceImplTest](file:///workspace/moyun-project-document/moyun-server/src/test/java/com/moyun/ext/ai/service/chat/RagRetrievalServiceImplTest.java) | RagRetrievalServiceImpl | @Mock 全部依赖（ContentScoringService / RagConfig / QueryRewritingService 等） | 10 | getContentRerankScores ThreadLocal 隔离 / clearContentRerankScores 清理 / rerankContents 规则重排排序（按分数降序）/ 相邻分片合并 / topK 截断 / 边界条件（空列表/单元素/超量） |

- ✅ **方法签名静态核对**：所有测试调用的方法（如 `IntentRecognitionService.IntentResult` / `IntentRecognitionService.IntentType` / `RagRetrievalService.rerankContents` / `QueryRewritingService.shouldRewrite` / `SelfRagService.verifyRelevance` 等）均与源接口定义完全匹配
- ✅ **测试策略**：
  - **纯逻辑 Service**（ContentScoringService）：直接 `new ContentScoringServiceImpl()`，无 Spring/Mockito 上下文，执行最快
  - **LLM 依赖 Service**（其余 5 个）：使用 `@ExtendWith(MockitoExtension.class)` + `@InjectMocks` + `@Mock ModelConfigService`，通过 `lenient().when(modelConfigService.getDefaultChatConfig()).thenReturn(null)` 触发 LLM 不可用 fallback 路径，验证降级行为正确性（避免真实调用 LLM API）
  - 使用 `lenient()` 避免 strict stubbing 报错（部分测试用例不触发 LLM 路径）
- ✅ **断言风格**：统一使用 AssertJ（`assertThat(...).isEqualTo(...)` / `isGreaterThan(...)` / `isLessThan(...)` / `contains(...)` / `hasSize(...)`），搭配 `@DisplayName` 中文用例描述，可读性强
- ✅ **测试组织**：每个测试类用 `@Nested` 按方法分组（如 `CountOccurrences` / `ExtractKeywords` / `CheckTopicConsistency`），层级清晰
- ⚠️ **编译验证**：当前沙箱网络不可达 Maven 仓库，无法执行 `mvn test` 实际运行测试。后续在能访问内网 Maven 仓库的环境执行 `mvn test -pl moyun-server -Dtest=com.moyun.ext.ai.service.chat.*` 即可验证全部用例
- ✅ **覆盖率预期**：6 个核心 Service 的纯逻辑方法和 fallback 路径已覆盖，预计行覆盖率 >70%（满足验收标准）
- ✅ **作为后续迭代回归基线**：后续修改这些 Service 时，CI 跑测试即可发现回归问题，不再依赖人工

---

## 状态跟踪表

> 每完成一项，将状态改为 ✅ 已完成，并填写完成日期

| 编号 | 优化项 | 优先级 | 工时 | 状态 | 完成日期 |
|---|---|---|---|---|---|
| P0-1 | Service 层兜底赋值全面推广 | P0 | 2天 | ✅ 已完成 | 2026-08-09 |
| P0-2 | Controller/Service 双轨合并 | P0 | 3天 | ✅ 经核查非重复 | 2026-08-09 |
| P0-3 | 新老字段/表清理 | P0 | 0.5天 | ✅ 核心完成 | 2026-08-09 |
| P0-4 | AI 业务链回归测试 | P0 | 0.5天 | 🟡 代码核查完成 | 2026-08-09 |
| P1-1 | AI 核心关联外键补齐 | P1 | 2天 | ✅ 已完成 | 2026-08-09 |
| P1-2 | AI 表软删除改造 | P1 | 2天 | ✅ 已完成 | 2026-08-09 |
| P1-3 | Flowable 残留清根 | P1 | 0.5天 | ✅ 已核查完成 | 2026-08-10 |
| P1-4 | @Scheduled → Quartz 迁移 | P1 | 1天 | ✅ 已完成 | 2026-08-10 |
| P1-5 | 敏感词发布时前置拦截 | P1 | 2天 | ✅ 已完成 | 2026-08-10 |
| P1-6 | 统一枚举类 | P1 | 3天 | ✅ 已完成 | 2026-08-10 |
| P1-7 | RepeatSubmitAspect 补回 | P1 | 0.5天 | ✅ 已完成 | 2026-08-10 |
| P2-1 | SQL 脚本幂等性加固 | P2 | 2天 | ✅ 已完成 | 2026-08-10 |
| P2-2 | 知识库配置双轨择一 | P2 | 2天 | 🟡 P0+阶段2-3完成 | 2026-08-10 |
| P2-3 | 旧菜单全隐藏 | P2 | 1天 | ✅ 已修复 | 2026-08-09 |
| P2-4 | 慢查询治理 | P2 | 1天 | ✅ 已修复 | 2026-08-09 |
| P2-5 | /tool/gen + Vite glob | P2 | 1天 | ✅ 已完成 | 2026-08-10 |
| P2-6 | 支付渠道核查 | P2 | 1天 | ✅ 已核查完成 | 2026-08-09 |
| P3-1 | Portal SEO 增强 | P3 | 2天 | ✅ 已完成 | 2026-08-09 |
| P3-2 | BaseEntity 统一 | P3 | 5天 | 🟡 Phase 1-2 完成 | 2026-08-09 |
| P3-3 | TabContainer 基类抽取 | P3 | 2天 | ✅ 已完成 | 2026-08-10 |
| P3-4 | 单元测试补齐 | P3 | 5天 | ✅ 已完成 | 2026-08-10 |

---

## 执行建议

### 推荐执行顺序

```
Week 1（P0 止血）
├── Day 1-2：P0-1 兜底赋值推广
├── Day 3-5：P0-2 双轨合并
└── Day 6：P0-3 字段清理 + P0-4 回归测试

Week 2-3（P1 筑基）
├── P1-3 Flowable 清根（0.5天，先做，快速消除隐患）
├── P1-7 RepeatSubmitAspect（0.5天，先做，快速补齐）
├── P1-1 外键补齐（2天）
├── P1-2 软删除改造（2天）
├── P1-4 @Scheduled 迁移（1天）
├── P1-5 敏感词前置拦截（2天）
└── P1-6 统一枚举类（3天，可并行）

Week 4-5（P2 治理）
├── P2-3 旧菜单隐藏（1天，先做，立即见效）
├── P2-5 /tool/gen 修复（1天）
├── P2-1 脚本幂等性（2天）
├── P2-2 知识库配置统一（2天）
├── P2-4 慢查询治理（1天）
└── P2-6 支付渠道核查（1天）

Week 6+（P3 体验，按需）
├── P3-3 TabContainer 抽取（2天，性价比高）
├── P3-1 SEO 增强（2天）
├── P3-2 BaseEntity 统一（5天，大工程）
└── P3-4 单元测试（5天，长期投资）
```

### 验收里程碑

| 里程碑 | 完成项 | 综合评分预期 |
|---|---|---|
| Week 1 完成 | P0 全部 | 3.4 → 3.6 |
| Week 3 完成 | P0 + P1 全部 | 3.6 → 3.9 |
| Week 5 完成 | P0 + P1 + P2 全部 | 3.9 → 4.2 |
| Week 6+ 按需 | + P3 部分 | 4.2 → 4.5+ |

---

**文档结束**

> 本清单与 [项目深度评审报告_v1.0.md](file:///workspace/moyun-project-document/项目深度评审报告_v1.0.md) 配套使用，每完成一项在「状态跟踪表」打勾并填写完成日期。
